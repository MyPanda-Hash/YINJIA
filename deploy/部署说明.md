# YINJIA-MES 服务器部署与热更新手册（Windows · 实战版）

> 本文档由 2026-09-08 首次真实部署全程沉淀,持续更新。
> **新对话接手时:只需阅读本文件即可衔接所有运维操作。**

## 服务器档案(关键信息)

| 项 | 值 |
|---|---|
| 公网 IP | **36.140.66.163** |
| 系统地址 | **http://36.140.66.163:8090** |
| RDP 远程桌面 | `36.140.66.163:3389`(主机名 `ECS-38508784`) |
| RDP 登录名 | `Administrator`(本地管理员;**密码不入库**,开发机凭据管理器已保存 `TERMSRV/36.140.66.163`) |
| 安装目录 | `C:\yinjia\` |
| 操作系统 | Windows Server (移动云 ECS) |
| 数据库 | SQL Server 2019 Express (MSSQL15, localhost:1433) |
| Java | Temurin 21 (PATH 已配置) |
| 管理员账号 | `admin / 123456`(2026-09-10 用开发机库整体覆盖后回到首次启动的种子密码;登录后请自行改密) |
| 应用账号 | yinjia / Yinjia@2026 (SQL Server 登录) |
| 开机自启 | 任务计划 YINJIA-MES → start-service.bat |

## 本地开发机档案

| 项 | 值 |
|---|---|
| 仓库路径 | `C:\INCER\YINJIA-MES` |
| 前端目录 | `C:\INCER\YINJIA-MES\frontend` |
| 后端目录 | `C:\INCER\YINJIA-MES\backend` |
| 本地运行 | `http://localhost:8090`(本地 SQL Server 同库 HSDZ_MES) |
| JDK(编译用) | `C:\Program Files\Java\jdk-24\bin\`(javac 编译 --release 17) |
| 本地 DB 登录 | yinjia / Yinjia@2026 或 Windows 集成认证(DDL) |
| 热更新包输出 | `C:\INCER\YINJIA-MES\deploy\app.jar` |

> **远程登录怎么做、连不上怎么查、以及"隔着 RDP 自动操作服务器"的完整配方 →
> 见末尾「七、远程登录服务器与自动化远程操作」。**
> 只有 3389(RDP) 和 8090(应用) 两个端口对外,没有命令行直连服务器的路。

---

## 一、首次部署(从零到上线,已完成,仅存档)

### 1. 传输部署包
RDP 远程桌面里直接复制粘贴 zip 到服务器,解压到 `C:\yinjia\`。

### 2. 安装 SQL Server
SQL Server 2019/2022 Express「基本」安装 + SSMS。

### 3. 恢复数据库(自动探测数据目录版)
SSMS 连 localhost(Windows 身份验证),整段执行:

```sql
USE master;
GO
IF DB_ID(N'HSDZ_MES') IS NOT NULL
    EXEC(N'ALTER DATABASE [HSDZ_MES] SET SINGLE_USER WITH ROLLBACK IMMEDIATE; DROP DATABASE [HSDZ_MES];');
GO
DECLARE @data NVARCHAR(400) = CAST(SERVERPROPERTY('InstanceDefaultDataPath') AS NVARCHAR(400));
DECLARE @sql NVARCHAR(MAX) = N'RESTORE DATABASE [HSDZ_MES] FROM DISK = N''C:\yinjia\HSDZ_MES.bak'' WITH RECOVERY, '
    + N'MOVE N''ASPSMT''     TO ''' + @data + N'HSDZ_MES.mdf'', '
    + N'MOVE N''ASPSMT_log'' TO ''' + @data + N'HSDZ_MES_log.ldf''';
PRINT N'使用数据目录: ' + @data;
EXEC(@sql);
GO
IF NOT EXISTS (SELECT 1 FROM sys.sql_logins WHERE name = N'yinjia')
    CREATE LOGIN [yinjia] WITH PASSWORD = N'Yinjia@2026', CHECK_POLICY = OFF;
USE [HSDZ_MES];
ALTER USER [yinjia] WITH LOGIN = [yinjia];
EXEC sp_addrolemember N'db_datareader', N'yinjia';
EXEC sp_addrolemember N'db_datawriter', N'yinjia';
EXEC sp_addrolemember N'db_ddladmin',  N'yinjia';
```

> **踩坑**:恢复前必须 `USE master`;MOVE 路径用 SERVERPROPERTY 自动探测,不要手写 MSSQL15/MSSQL16。

### 4. 安装 Java 21
Temurin 21 MSI,默认勾选 Add to PATH。

### 5. 生成启动脚本(PowerShell,防编码坑)
> **踩坑**:bat 含中文注释必须 GBK/ANSI 编码,UTF-8 的 bat 在中文 cmd 里必炸。以下生成纯 ASCII 脚本:

```powershell
@'
@echo off
cd /d "%~dp0"
title YINJIA-MES backend :8090
java -jar app.jar
echo.
echo [EXITED] backend stopped - check errors above
pause
'@ | Set-Content C:\yinjia\start.bat -Encoding ASCII

@'
@echo off
cd /d "%~dp0"
:loop
java -jar app.jar
echo [%date% %time%] exited, restart in 5s...
timeout /t 5 /nobreak >nul
goto loop
'@ | Set-Content C:\yinjia\start-service.bat -Encoding ASCII
```

### 6. 放行外网(两道闸)
```powershell
# ① Windows 防火墙(管理员 PowerShell)
New-NetFirewallRule -DisplayName "YINJIA-MES 8090" -Direction Inbound -Protocol TCP -LocalPort 8090 -Action Allow -Profile Any
# ② 移动云安全组(控制台→安全组→入方向):TCP 8090 / 0.0.0.0/0
```

### 7. 上线加固
1. 改 admin 密码(默认 123456)
2. 换 JWT 密钥:`start.bat` 里取消注释并填 64 位随机十六进制串,本机生成:
   ```powershell
   -join ((1..32) | ForEach-Object { '{0:x2}' -f (Get-Random -Maximum 256) })
   # 校验:输出为 64 位、匹配 ^[0-9a-f]{64}$
   ```
3. 开机自启:`schtasks /Create /TN "YINJIA-MES" /TR "C:\yinjia\start-service.bat" /SC ONSTART /RU SYSTEM /RL HIGHEST /F`

---

## 二、数据库与代码同版本发布(含 DDL/数据变更) ★ 最易踩坑 ★

> **硬纪律(2026-09-08 定,不可绕过):热更新换包永不碰数据库;凡是版本含表结构/元数据/数据变更,
> 必须先迁库、再换包,两条流程严格分离。**
>
> 为什么必须成对:新 `app.jar` 的代码会读写新表/新列。服务器库落后一步 → 启动时 SQL 报错
> (`无效的列名` / `对象名无效`)或面板直接 500。**先迁库、后换包,顺序不能反。**

### 先判断走哪条路

| 走法 | 适用 | 代价 |
|---|---|---|
| **A. 全量恢复备份**(实测路径) | 服务器数据可被本机覆盖(一次上线/演示/重装) | 覆盖服务器库 = 丢掉服务器上新产生的数据 |
| **B. 增量迁移脚本**(最小改库) | 服务器已在生产、有真实业务数据 | 需逐条核对脚本、幂等性 |

### A. 全量恢复(已实测走通的流程)

```
本地开发机                                服务器(RDP)
━━━━━━━━━━                                ━━━━━━━━━━
① 出一份最新全量备份
   sqlcmd -S localhost -d HSDZ_MES -E -Q
     "BACKUP DATABASE [HSDZ_MES] TO DISK=
      'C:\INCER\YINJIA-MES\deploy\HSDZ_MES.bak'
      WITH FORMAT, INIT, STATS=10"
   → deploy\HSDZ_MES.bak(约 54 MB)          ② 粘贴 HSDZ_MES.bak 到 C:\yinjia\
                                            ③ SSMS 连 localhost(Windows 身份验证)
                                               跑「一、3」那段自动探测版 RESTORE
                                               (含 DROP 残留 + 建 yinjia 登录 + 授权)
                                            ④ 服务器浏览器 http://localhost:8090
                                               admin 登录 → 抽查一个面板确认数据在
                                            ⑤ 再走「三、热更新」换 app.jar
                                            ⑥ 本机实测 http://36.140.66.163:8090 复核
```

### B. 增量迁移(服务器数据不能动时用这条)

> ℹ️ **现状提示(2026-09-10 实测)**:开发库 `HSDZ_MES` 里**没有 `yj_schema_log` 表**——
> 说明历史迁移全是 `sqlcmd` 手工执行、**从未记录过**。所以**第一次**在开发库或服务器跑
> `DbSync`/全量初始化时,清单里 116 条脚本会被**当新脚本全部执行一遍**(脚本幂等,理论安全)。
> 若不想让它们重跑(库就是当前版本),先按下面第 4 步跑 `baseline` 把它们只标记为已执行。

1. 本机生成/确认本次涉及的迁移脚本(都在 `tools/`,如 `migrate-phase23-forms.sql`);
2. **登记清单**:确认脚本已写进 `tools/db-migrations.txt`(该文件是"唯一权威顺序",
   `DbSync.java` 增量同步与 `DbInit.java` 全量初始化都只认它);
3. 两个执行口径,任选:
   - **服务器上有仓库副本** → 在服务器跑 `tools\pull-sync.bat`:`git pull` + `DbSync` 按清单
     **只执行新增/内容有变化的脚本**(幂等,可重复);
   - **服务器上没有仓库** → SSMS 手工执行:把脚本内容贴进查询窗口,或
     `sqlcmd -f 65001 -S localhost -d HSDZ_MES -E -i "C:\yinjia\tools\migrate-xxx.sql"`
     (`-f 65001` 必须加,否则中文乱码);
4. **已在服务器手工跑过、不想被清单重跑** → 在服务器 `tools\` 目录下用基线模式,把清单里
   尚未记录的脚本**只标记为已执行、不动数据**:
   ```powershell
   cd C:\yinjia\tools     # 必须在此目录执行:DbSync 按"当前目录"解析 db-migrations.txt
   java -cp <mssql-jdbc.jar> DbSync.java baseline
   ```
   DbSync 三个模式:`(默认)` 增量同步 · `baseline` 仅标记 · `run <脚本名>` 无视记录强制执行;
   执行的脚本名 + 内容 SHA-256 记在 `yj_schema_log`,内容一变就会重跑(故脚本必须幂等)。
5. 验证 → 再换 jar(同 A 的 ⑤⑥)。

### 收尾硬规矩

```sql
-- 重大变更前先备份(服务器本地):
BACKUP DATABASE [HSDZ_MES] TO DISK = N'C:\yinjia\backup\pre-update.bak' WITH FORMAT, INIT;
```

> 🔴 **清单禁入名单**:`tools\_doc_part3_cleanup.sql` 是**一次性清库脚本**——它 `DELETE` 掉
> `bl_/bd_` 的 dispatch / manu_order / material_out / outsource_order / pu_order 中
> `asp_user1='migration'` 的**全部业务单据行**。它**不在** `db-migrations.txt` 里,**绝不要登记进去**,
> 也不要随手对服务器库执行。其余 `migrate-*.sql` 均幂等(IF NOT EXISTS / 条件 UPDATE / DELETE+INSERT),
> 库内重跑安全。

---

## 三、热更新(日常版本升级,不动数据库) ★ 新对话必读 ★

> 本节只解决"换代码"。**若本次版本含数据库变更,先做第二节,再回来做本节。**

### 完整流程(3 步)

```
本地开发机                          服务器(RDP)
━━━━━━━━━━                        ━━━━━━━━━━
① 跑一键打包脚本
   pwsh -File C:\INCER\YINJIA-MES\tools\build-hot-update.ps1
   → 产出 deploy\app.jar
                                    ② 复制 app.jar 到
                                       C:\yinjia\update\app.jar
                                       (RDP 窗口直接粘贴)
                                    ③ 双击 C:\yinjia\update.bat
                                       → 自动停/备/换/启
                                       → Started MesApplication ✓
```

### 本地一键打包脚本

```powershell
pwsh -File C:\INCER\YINJIA-MES\tools\build-hot-update.ps1
```

脚本自动执行(约 30 秒):
1. 前端 `npm run build`
2. 后端全量编译(javac --release 17 -parameters, fat-jar classpath)
3. 同步 dist → static
4. 组装 fat-jar(所有 .class + static)
5. 重启本地开发服务
6. 输出热更新包到 `deploy\app.jar`

> 脚本首次运行会解包 fat-jar 提取 classpath(jar-inspect 目录,后续复用)。

### 服务器热更新

1. RDP 远程桌面,把 `deploy\app.jar` **粘贴到** `C:\yinjia\update\`(覆盖旧的)
2. **双击 `C:\yinjia\update.bat`** → 自动:
   ```
   [1/4] stopping backend...     ← taskkill java
   [2/4] backing up...           → app.jar.bak
   [3/4] installing new jar...   ← update\app.jar → app.jar
   [4/4] starting backend...     ← start.bat
   ```
3. 新窗口出现 `Started MesApplication` + `HikariPool Start completed` = **更新完成**

### 回滚

```cmd
copy /Y C:\yinjia\app.jar.bak C:\yinjia\app.jar
```
然后重启(taskkill java → start.bat)。

### 数据库迁移(如涉及表结构变更)

> 热更新**不动数据库**。本次版本若含表结构/元数据/数据变更,**先做「二、数据库与代码同版本发布」再回来换包**。
> 迁移清单唯一权威顺序:`tools/db-migrations.txt`(`DbSync`/`DbInit` 都按它执行);执行方式见「二、B」。

---

## 四、代码提交规范(AGENTS.md 红线)

- **单任务单提交**:一个任务一个 commit,格式 `<type>: <中文摘要>`
- type ∈ feat / fix / docs / refactor / chore / db / i18n
- 涉前端改动必须 `npm run build` 通过再提交
- 涉数据库改动必须幂等迁移 SQL 已验证

---

## 五、排查速查表(实战版)

| 症状 | 处理 |
|---|---|
| `Port 8090 was already in use` | `taskkill /IM java.exe /F` 再启动 |
| bat 报 `'dp0"' 不是内部或外部命令` | bat 是 UTF-8 → 用 PowerShell 重生成 ASCII 版 |
| `'java' 不是内部或外部命令` | JDK 没装/PATH 未生效 → 新开 cmd |
| RESTORE 报"正由此会话使用" | 脚本开头缺 `USE master;` |
| RESTORE 报目录查找失败 | 用自动探测版脚本(不手写路径) |
| **外网打不开(防火墙+安全组都放行了)** | **先核对服务器真实公网 IP**:服务器上 `curl http://ip.3355.cn`,或移动云控制台实例页核对。**IP 抄错时所有放行都"不生效",站长工具也显示关闭**(2026-09-08 实测:一直测的 .136 是错的,.163 才是真身) |
| 外网打不开(IP 已核对) | ① Windows 防火墙规则(netsh 验证) ② 移动云安全组入方向 ③ 移动云部分产品线实例页还有独立**「防火墙」标签**,与安全组是**两道闸** ④ 规则是否加在**该实例实际绑定的**安全组上(加错组是高频坑) ⑤ 源地址别选成"安全组内网",TCP 8090 源 `0.0.0.0/0` |
| 数据库连不上 | SQL Server 服务没起 / yinjia 登录未建 |
| **服务起不来(SQL 报"无效的列名/对象名无效")** | 库落后于代码 → 先按「二」迁库(或恢复新 bak)再换 jar |
| **sqlcmd 执行中文脚本输出乱码** | 加 `-f 65001`:`sqlcmd -f 65001 -S localhost -d HSDZ_MES -E -i tools\migrate-xxx.sql` |
| **某迁移脚本好像"没被部署执行"** | 它没登记进 `tools/db-migrations.txt` → 按「二、B」补登记(`DbSync`/`DbInit` 只认清单) |
| javac 报 ButtonService 找不到 | 缺 import 或 classpath 不含 jar-inspect → 用 build-hot-update.ps1(自动处理) |
| javac 编译的 Controller 端点 400 | 编译缺 `-parameters` 旗标(Spring @PathVariable 需要) |
| jar 更新后行为没变 | 服务器上旧 java 进程没死干净 → taskkill 后再跑 update.bat |

---

## 六、架构速查(新对话快速上手)

```
前端: Vue 3 + Element Plus + vue-i18n (frontend/src/)
后端: Spring Boot 3.5 + JdbcTemplate (backend/src/main/java/com/yinjia/mes/)
数据库: SQL Server HSDZ_MES (localhost:1433)
  ├ bs_bom (物料清单)
  ├ bs_inv (存货档案;含 是否检验/检验方式 两列)
  ├ bs_inv_price (存货价格本)
  ├ bs_wh (仓库档案;CK04 成品/CK06·CK07 隔离仓)
  ├ kucun (库存台账)
  ├ yj_doc_status (单据状态机: 审批/归档/修改/删除)
  ├ yj_doc_modify_log (修改记录, 滚动3条)
  ├ yj_form_approval (审批留痕)
  ├ yj_message (业务事件消息/通知中心)
  ├ qr_batch_registry (批号注册表, 批号=入库日期+3位流水)
  ├ wo_order / wo_progress (生产工单/报工累计)
  ├ wo_stage_report / wo_material_pick / wo_line_stock (五工序: 报工/领料/线边库存)
  ├ yj_lot_seq / erp_imp_log / erp_imp_row (批号流水/ERP导入通道与审计)
  ├ v_wo_schedule / v_wo_kit (排产视图/工单齐套)
  ├ v_lot_trace (批号追溯: 八事件 UNION 时间线)
  ├ yj_panel / yj_field (面板元数据, 中文数据键)
  ├ yj_role_panel (角色面板权限)
  └ yj_user / yj_role / yj_dept (组织架构)

文件面板(文书式, 21个): DOC_ARCHIVE_PANELS 常量(ButtonService.java)
状态链: 已作废 > 已中止 > 删除申请中 > 修改申请中 > 审批中 > 修改中 > 已归档 > 已审核 > 草稿

面板渲染器:
  RecordSheetPanels.vue → 产品文件7张 + 数据记录表7张 + 实验室4张 + 生产工序单据29张(配置驱动: recordSheetConfigs.js)
  DocSheet.vue → 立项申请表 + 项目实施计划(配置: docSheetConfigs.js)
  DataRecordSheet.vue → 功能性滤效
  ProgressControlSheet.vue → 项目进度查询
  BomMasterDetail.vue → 物料清单(BOM/BOM_FWD/BOM_REV)

ERP 同步(独立于 app.jar): jdy-sync(金蝶云·星辰销售订单,每5分钟计划任务)
  本机 D:\jdy-sync · 服务器 C:\...\jdy-sync · 部署包 deploy\jdy-sync.zip + jdy-sync-setup.bat
```

---

## 七、远程登录服务器与自动化远程操作 ★ 2026-09-10 实测沉淀 ★

> 本节解决两件事:**人**怎么连上服务器;**AI/脚本**怎么在只有 RDP 一条通道的前提下把活干完。

### 1. 人工远程登录

| 项 | 值 |
|---|---|
| 地址 | `36.140.66.163:3389`(主机名 `ECS-38508784`,Windows Server,移动云 ECS) |
| 登录名 | `Administrator`(本地管理员) |
| 密码 | **不入库**。开发机凭据管理器已保存 `Domain:target=TERMSRV/36.140.66.163`,`mstsc` 直接免密登录 |

```powershell
# ① 确认开发机上凭据还在(只列目标,不显示密码)
cmdkey /list | Select-String 'TERMSRV'

# ② 直连
mstsc /v:36.140.66.163
```

> 🔴 **`.rdp` 文件里不要写密码。** `password 51:b:...` 是绑定当前机器的加密串,换机即失效,
> 而且属于敏感信息(AGENTS.md 红线)。统一依赖凭据管理器。

**连不上时按这个顺序查:**

1. 端口通不通(从开发机):`Test-NetConnection 36.140.66.163 -Port 3389`
2. 公网 IP 有没有抄错 —— 见「五、排查速查表」的 IP 核对法(2026-09-08 实测踩过 `.136`/`.163`)
3. 移动云安全组入方向是否放行 **TCP 3389**(它与 8090 是**分别**放行的)
4. Windows 防火墙入站规则

**登录后第一件事:确认自己是 sysadmin。** 后续备份/还原全靠它:

```bat
sqlcmd -S localhost -E -h -1 -W -Q "SET NOCOUNT ON; SELECT 'OK ' + CAST(IS_SRVROLEMEMBER('sysadmin') AS varchar(1))"
```

### 2. 对外只开了两个端口(2026-09-10 实测)

| 端口 | 用途 | 实测 |
|---|---|---|
| 3389 | RDP | **开放** |
| 8090 | 应用 | **开放** |
| 5985 / 5986 | WinRM | 不通 |
| 445 | SMB | 不通 |
| 22 | SSH | 拒绝 |
| 1433 | SQL Server | 未对外暴露 |

**结论:不存在"命令行直连服务器"的路。** 远程只有两条通道 ——
应用侧走 HTTP 8090,运维侧走 RDP 图形界面。所以下面这套"隔着 RDP 屏幕操作"的配方不是可选项。

### 3. `.rdp` 关键参数(自动化操作的前提)

```ini
full address:s:36.140.66.163      ; 不要带 :3389 —— 带了就和凭据管理器里的 TERMSRV/36.140.66.163 对不上
prompt for credentials:i:0        ; 直接用已保存的凭据,不弹输入框
authentication level:i:0          ; 不弹证书警告,否则无人值守时会卡在模态框上
drivestoredirect:s:C:\            ; ★ 把开发机 C 盘映射进服务器 → 服务器里可访问 \\tsclient\C\...
redirectclipboard:i:1             ; 剪贴板互通
keyboardhook:i:1                  ; ★ Windows 组合键发给远程(默认是发给本机的)
screen mode id:i:1                ; 窗口模式,别全屏(全屏会盖住开发机,截图/操作都不方便)
```

生成后放 `C:\INCER\DSHTemp\server.rdp` 即可(机器相关,**不入库**)。

### 4. 为什么 `\\tsclient\C\` 是整套方案的关键

打开 `drivestoredirect` 后,**服务器上的进程能直接读写开发机的 C 盘**,于是:

- **不用传文件**:服务器上直接执行 `\\tsclient\C\yj-deploy\deploy.bat`
- **日志自动回传**:脚本把日志写到自己的 `%~dp0logs\`(即重定向盘),跑完文件就已经在开发机上了
  —— AI 与人**直接读文件**,不依赖截图识字(截图识别远程小字极不可靠)
- ⚠️ 这条路径对**服务器上的 cmd / sqlcmd / java 可见,但 SQL Server 服务账号看不到**
  (服务账号不在这个登录会话里)。所以**备份文件必须先写到服务器本地盘,再用 `copy` 搬到重定向盘**。

**命名约束:凡是要"手敲"的路径必须纯 ASCII。** 中文路径没法用按键输入进 cmd,
所以桌面那份包额外挂了一个 ASCII 短路径:

```
C:\yj-deploy   →  (junction)  C:\Users\x1787\OneDrive\Desktop\YINJIA-MES-DEPLOY-20260910
```

命令行里只用 `\\tsclient\C\yj-deploy\...`,不要用中文目录名。

### 5. 自动化操作的两条硬约束(都踩过)

**① `.bat` 必须纯 ASCII。** zh-CN 的 cmd 按 GBK 解析批处理,UTF-8 中文 `.bat` 会直接崩
(`start.bat` 顶部就写着这条)。要让用户看到中文,只能让它**输出重定向得到的文件**里的中文。

**② Win 组合键默认被本机截走。** 即使设了 `keyboardhook:i:1`,`SendKeys '#r'` 也未必能打开
远程的运行框(实测本机把 Win+R 吃了)。可靠做法是 **`Ctrl+Esc`** 开远程开始菜单 → 输入 `cmd` → 回车。
下面这套按键配方 **2026-09-10 实测可用**:

```powershell
$m = Get-Process mstsc
(New-Object -ComObject WScript.Shell).AppActivate($m.Id) | Out-Null
Start-Sleep -Milliseconds 800
[System.Windows.Forms.SendKeys]::SendWait('^{ESC}'); Start-Sleep -Seconds 3   # 远程开始菜单
[System.Windows.Forms.SendKeys]::SendWait('cmd');     Start-Sleep -Seconds 3   # 搜 cmd
[System.Windows.Forms.SendKeys]::SendWait('{ENTER}'); Start-Sleep -Seconds 5   # 开命令行
[System.Windows.Forms.SendKeys]::SendWait('\\tsclient\C\yj-deploy\deploy.bat'); Start-Sleep -Seconds 1
[System.Windows.Forms.SendKeys]::SendWait('{ENTER}')                            # 执行
```

**③ 永远不要盲敲。** 远程的前台窗口会变 —— 本次实测中一度把按键敲进了服务器上 SSMS 打开的
SQL 查询编辑器里。所以:

- 命令要**短、幂等、优先只读**;
- 工具一律做成 **CHECK / GO 两档**:先跑 CHECK(不改任何东西)看结果,确认后再 GO;
- **成败以回传的文件为准,不以截图为凭。**
- 开跑前顺手看一眼目标窗口:如果远程开着 SSMS 之类会吞按键的程序,先把它关掉/最小化。

### 6. 服务器上跑脚本的日志编码(统一钉死 UTF-8)

回传的日志如果在开发机上乱码,**失败检查会被静默吞掉**(计数恒为 0,守卫形同虚设)。所以:

- `sqlcmd`:`-f i:65001,o:65001`(读 UTF-8、写 UTF-8)
- `java`:`-Dfile.encoding=UTF-8 -Dstdout.encoding=UTF-8 -Dstderr.encoding=UTF-8`
  —— **三个都要**:DbSync 的正常输出走 `stdout`、失败信息走 `stderr`(实测 `System.out` 与
  `System.err` 在 zh-CN 上默认都是 GBK);只设前两个,`[FATAL]` 行仍是 GBK
- 判定逻辑用**纯 ASCII 标记**(如 `RESULT: ALL-OK` / `MISSING-n`),
  **不要用 `✓`/`✗` 去计数** —— 编码一错计数就恒为 0
- `sqlcmd` 的 `-h` 与 `-y 0` 互斥、`-W` 与 `-y 0` 互斥;导出长文本(视图定义)时用 `-y 0` 且不加前两者
- `.bat` 里 `echo ... -> 文件` 的 `>` 会被 cmd 当成重定向,把目标文件截断(实测踩过)

### 7. 现成工具(都是 CHECK / GO 两档,日志自动回传到 `logs\`)

| 脚本 | 档位 | 作用 |
|---|---|---|
| `deploy\deploy-server.bat` | 无参 = 只读自查<br>`GO` | 前置检查 + 库缺项自查 + 迁移台账/视图定义取证<br>停应用 → 备份库 → baseline 登记 → 补跑幂等脚本 → 复验 → 换 app.jar → 起应用 |
| `deploy\push-db.bat` | 无参 = 只读预检<br>`FIX`<br>`GO` | 检查 `.bak` 可达 + `sqlcmd -E` 是否 sysadmin<br>修跨服务器还原的孤儿登录并重启后端<br>**整体覆盖服务器库**(先备份,失败硬停) |

两条安全闸(实测有效,保留):

- **备份失败就硬停** —— 绝不在没有回滚点的情况下改库
- **复验仍有缺项就不换 app.jar** —— 避免"库落后但代码已更新"的故障重演

### 8. 跨服务器还原的必踩坑(务必记住)

把别的机器上的 `.bak` 还原到本机后,**库里的用户带着原机器的 SID,与本机登录对不上**:

- 直接症状:连接报 `用户 'yinjia' 登录失败`
- **更阴的连锁**:应用启动时 Tomcat 先监听(健康检查拿得到 200,是**假阳性**),
  随后启动期缓存预热去查库 → 登录失败 → 上下文刷新失败 → **进程退出**,外网变成 connection refused
- 修复:`ALTER USER [yinjia] WITH LOGIN = [yinjia]` + 补 `db_owner`
  (已固化为 `tools/fix-db-logins.sql`,`push-db.bat` 还原后会自动执行)

> 教训:**健康检查返回 200 不等于应用可用** —— 那只证明静态页面能出。真正的验证要打一个
> 会查库的接口,例如 `POST /api/auth/login` 用正确账号返回 200 且签发 token。

---

— 本文档位于仓库 `deploy/部署说明.md`,由 grill-with-docs 会话维护。
— 最后更新: 2026-09-10(新增「七、远程登录服务器与自动化远程操作」;补「数据库与代码同版本发布」一节、IP 核对法、移动云双闸、sqlcmd 编码坑、架构速查扩表)
