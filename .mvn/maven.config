# 锚点文件:只用来让 Maven 的 maven.multiModuleProjectDirectory 指向仓库根。
# mvn.cmd / mvn 启动脚本会从当前目录向上找 `.mvn` 目录,找到就以它作为多模块根目录
# (mvn.cmd:142 `if exist ".mvn" goto baseDirFound`)。tools/settings.xml 的
# localRepository 依赖这个属性,从而无论从仓库根、backend 还是更深目录调用,
# 本地仓库都解析到 <仓库根>/.m2-repo —— 详见 tools/settings.xml 顶部注释。
#
# 本文件不含任何实际参数(Maven 3.9 起 .mvn/maven.config 支持 # 注释)。
# 不要删除:删掉后 .mvn 目录会从 git 里消失(空目录不被跟踪),锚点随即失效。
