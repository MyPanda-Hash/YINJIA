fix: 库存报表批号未同步——v_stock_movement 采购入库段改批次号优先(两账套)

用户报:库存报表里批次号没同步。查证:bl_purchase_in 上 115 行只有「批次号」(分批送料链路落的列,
如 20260923/20260924)有值而「批号」(金蝶同步列)空——v_stock_movement 只读 批号,这些行全部
显示「(未填批号)」(视图占位 24 行);其余 7 类行表无 批次号 列(链路未到),口径不变。
修复:migrate-inv-report-fields 采购入库段批号改 COALESCE(批次号, 批号, 占位)——与 kucun 台账
(StockLedgerService:批次号优先、批号兜底)同口径;两账套视图重建 + 重算成本 243 行
(inv_cost_ledger 批号冗余列随之刷新;分区键不含批号,成本金额不动)。
验证:「(未填批号)」占位大幅回落(24→真实值);探针 _q-batch-*/_q-batch-after 归档。