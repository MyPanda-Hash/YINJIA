# 技术债清单

> 规范来源:`docs/development/代码规范与防臃肿.md` 的 **E3 技术债台账**。
> 存量问题记在这里,每完成一个大版本清理一批;**新代码不新增债**。
> 每条尽量写清:现象 → 根因 → 证据 → 修复所需的前置条件。

---

## 1. 项目实施计划:立项申请 → 实施计划的字段自动带入未实现

**状态**:已定位根因,**暂不修复**(2026-09-10 决定)

**2026-09-11 复核**:仍存在(本轮只改了 RD_PLAN.负责人 只读,refMap 映射容器仍未落地)

**现象**:在项目实施计划里选「文档编号」(参照立项申请)后,只有 3 个通用字段被带回,
业务字段(`测试内容/测试产品打样要求/测试目标/测试条件/测试方法/测试标准`)一个都带不回来。

**根因**:后端 `PanelConfigService.buildRefMap` 只按**同名字段**生成映射。
服务器实测返回的配置是:

```
refMap: [{文件管理人→文件管理人},{密级→密级},{文件使用范围→文件使用范围}]
```

而两边字段名**完全不重叠**:

| 立项申请(RD_APPROVAL) | 实施计划(RD_PLAN) |
|---|---|
| 客户名 / 立项背景 / 机型及应用位置 / **滤芯炭棒规格或结构** / **项目开发目标** / 项目输出 / 开发周期要求 / 其它要求 / 申请立项人 / 申请立项日期 / 文件管理人 / 密级 / 文件使用范围 / 文档编号 | **项目名称** / 项目定级 / **测试内容** / **测试产品打样要求** / **测试目标** / **测试条件** / **测试方法** / **测试标准** / 负责人 / 编制日期 / 文件管理人 / 密级 / 文件使用范围 / 文档编号 |

**已有能力 vs 缺口**:前端 `core/ref/refCarry.js` 的 `applyRefCarry()` 已经支持消费
`ref.map / refMap` 数组;但 **`yj_field` 表里没有存放 refMap 的列** ——
也就是说这套跨面板映射能力目前**无处配置**。

**修复所需**:一张「源字段 → 目标字段」的显式映射表(业务决策,需用户给),
实现上建议在 `buildRefMap` 里加一段针对 RD_PLAN←RD_APPROVAL 的显式映射(不动表结构)。

---

## 2. RD_PROGRESS 控制列表:3 列仍未与元数据对齐

**状态**:已知,**按用户决定暂留**(2026-09-10)

`core/progress/progressColumns.js` 里标记为 `pendingAlign` 的三列,
其"显示名"被当作数据键,而 `rd_progress_detail` 里**没有对应物理列**,
所以这三列目前**只显示、不落库**:

| 界面表头 | 可能的物理列(未确认,按逐位对应推测) |
|---|---|
| 技术目标达成 | `谁来批准` |
| 是否市场转化 | `谁来检验` |
| 未转换原因 | `未批准原因` |

这三列在库里的现存数据全为 NULL,**没有数据佐证**,故未采纳推测。
`progressColumns.test.js` 用断言钉住了 `pendingAlign` 的数量与内容 ——
将来对齐时必须回来改测试,不会被悄悄绕过。

> 已对齐的 11 列见同文件;那次修复的根因是"前端拿显示名当数据键,
> 后端 `labelsToCols` 按元数据过滤后静默丢弃"(详见 git `1382dd5`)。

---

## 3. 迁移清单里混有已过期 / 具有破坏性的历史脚本

**状态**:**已解决**(2026-09-21 结清;此前 2026-09-10 记录、2026-09-15 部分处置)

**2026-09-21 复核与结清**(本节原描述已过时,以本段为准):

- 这批脚本**已在 2026-09-15 的破坏性重跑事故收口时就地中性化**(内容只剩 `PRINT`,不再有任何
  DDL/DML —— 见 `tools/db-migrations.txt` 2026-09-15 那一段登记,以及 git 历史)。原描述里
  「会删视图」「会 DELETE 面板」说的是**中性化之前的旧内容**,当时确认无误、现已不成立。
- 残留的两个真问题本轮一并处理:
  1. **8 个已中性化的空操作脚本仍留在清单里**(`cleanup-base-panels`、
     `fix-views-def/definitive/final/v2/v3/v4/v5-gen`):跑起来只是 PRINT,却让
     「清单里有什么」与「真跑了什么」对不上。已从清单剔除并 `git mv` 到 `tools/archive/`
     (原位留说明),清单条目 238 → 223。
  2. **7 处重复登记**(`migrate-rd-2026-design` ×3、`migrate-rd-progress-18cols`/`i18n-rd-2026-design`/
     `migrate-rd-prod-doclist`/`migrate-rd-sample-no`/`migrate-asm-proc-lib` ×2):作者本意是「重跑」,
     但 `DbSync` 按内容哈希跳过未变化脚本 —— **重复登记永远不会带来第二次执行**,只会谎报执行顺序。
     已删除后置条目并原位留说明。
- `migrate-rd-cleanup.sql` 已不在清单里(游离脚本),符合「生产永不执行」的定位;`deploy-all.sql` 同样游离。
- 「给 DbSync 加危险脚本二次确认」以更通用的形式落地:**检出脚本里的无条件 `USE <库>` 且与本次目标库
  不符即中止(exit 3)** —— 这拦的是一类真实事故(见下)。

**本轮新发现并已修复**(原清单未记录,同属「改错库/改错东西」家族):

- **98/238 个迁移脚本开头写无条件 `USE HSDZ_MES;`**,实测「连 HSDZ_MES_TEST 执行 `USE HSDZ_MES`」会把
  会话切到正式库 ⇒ `DbSync` 承诺的「`YINJIA_SQL_DB=HSDZ_MES_TEST` 给测试账套补迁移,不碰生产库」
  **被静默打穿**:实际改的是生产库、测试库一条没补,且不报错。98 个脚本已改为守卫形式
  `IF DB_NAME() = N'master' USE HSDZ_MES;`(实测:连测试库不被切走、连 master 仍切正式库、同批次后续
  语句照常解析);**不能直接删该行** —— `yinjia` 登录默认库是 `master`,删了会在 master 里建表。
- 工具:`tools/archive/_list-migrations.mjs`(清单体检,须字节级解析)、
  `_guard-use-hsdz.mjs`(批量改守卫形式)、`_tidy-migrations.mjs`(删重复/剔空操作)。

**原始记录(2026-09-10,保留作证据)**:

`tools/db-migrations.txt` 当时 128 条,有若干脚本在**中性化之前**于当时表结构上重跑会造成破坏,
或本身就是一次性建库/测试数据脚本:

- **`fix-views-final.sql` 会删视图**:它先 `DROP VIEW` 再 `CREATE`,
  而脚本已过期(报「列名 '存货' 无效」,同样问题的还有 `fix-views-v2/v3/v4/ultimate/definitive/v5-gen`、
  `deploy-all.sql`)→ 一旦执行,报表视图被删且**建不回来**。
  2026-09-10 实测踩过一次(本机 19 个报表视图被删,后从服务器导出定义补回)。
- **`cleanup-base-panels.sql`** 会 `DELETE` 面板(GFDA/CKDA/YWYDA),且 ALTER 无存在性判断。
  (2026-09-16 又实发一次删尽 GFDA 字段,由 `migrate-restore-gfda.sql` 收口。)
- **`migrate-rd-cleanup.sql`** 是一次性测试数据清理,生产上永不执行。
- `_so_data.sql` / `_doc_part3_data.sql` / `dispatch-*` 等属建库种子或历史补丁。

**影响**:这些脚本只要**未登记**就会被 `DbSync` 默认 sync 模式执行 ——
这正是 2026-09-10 本机库受损的直接机制(已修复并复盘)。
**任何情况下都不要在已有数据的库上跑默认 `sync`。**

---

## 4. RD_INSP_PLAN 产品功能类别:字段的参照取值域 ≠ 自动填充的值域

**状态**:已知,**待业务定见**(2026-09-20 出货检验计划表重排时暴露)

**现象**:本面板 `产品功能类别`(`yj_field` seq 105,`place=header`)是**既有**的
`参照 → RD_PROD_INFO.产品功能类别`;而 `RD_PROD_INFO.产品功能类别` 是下拉框,
字典只有 `除余氯 / VOC / 重金属`(性能功能词)。
但设计纸《出货检验项目控制计划.xlsx》把这一格标为「自动填充规格书」,
`autoFillSpec` 便按设计把它填成**规格书**的 `产品类别`
(20 个品类词:普通炭棒 / 高精度炭棒 / … / 烧结矿化棒)。
**两者取值域不相交** ⇒ 填进去的值落在参照键域之外(能显示、能打印,
但参照弹窗选不中该项,该字段作为参照的一致性被破坏)。

**根因**:设计这一格只有「自动填充规格书」一个来源、**没有手选路径**,
而字段元数据还留着参照 + 弹窗。配置(文本格 + 自动填充)与元数据(参照)两半不一致。

**修复所需**:业务先定这一格的**权威口径** ——

- 若照设计(推荐):该格只由规格书带入 ⇒ 把 `RD_INSP_PLAN.产品功能类别` 的
  `data_type` 改为 `文本`、清空 `ref_panel/ref_field/display_field`,配置一行不动;
- 若要以 `除余氯/VOC/重金属` 为准 ⇒ 规格书侧需先有该口径的列(现无),
  且这一格不再由规格书自动填,需回到手选 —— 与设计原文冲突。

⚠ 该字段是既有共享元数据,**未擅自改**,以免影响本面板通用表单的渲染。

---

## 5. 出货检验标准库(insp.plan)存量值与新词表的口径不符

**状态**:已知,未清洗(2026-09-20)

重排后本面板列名与下拉都已按设计对齐,但**库存行数据**还是旧口径:

- **`控制方法` 存的是"控制分类"不是"检验方法"**:库里 13 行该列放的是
  `常规抽检` / `型式检测报告` 这类**管控方式**,而设计要的短检验方法文本
  (如"按 GB/T 5750 检验")实际存放在 `检测仪器` 列。列名换了,存量值语义没换。
- **`检测频率` 的存量值与新下拉词表不符**:下拉词表 = 设计给的 3 项
  (`1PCS/一个生产批次` / `生产量*1%` / `型式检验`),而库里存量值是
  `每批次` / `型式检验半年一次` —— 勾选落表后与下拉词表对不上。
- **3 行 `碱性性能测试` 在 `spec.test` 库里没有对应条目**(13 行 insp.plan 中),
  即这几个检验项目在规格书侧无源可溯。

**影响**:新建单从标准库勾选时,带出的文字与下拉词表不自洽;不阻塞功能,属数据口径债。

**修复所需**:业务确认口径后清洗 `yj_std_lib WHERE lib_code='insp.plan'` 的存量值
(建议按 `检测仪器 → 控制方法` 做一次搬迁,再逐行核对 `检测频率`)。
