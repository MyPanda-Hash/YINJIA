# 方案:采购订单分批送料与批次号贯通

> 目标链路:**采购订单 PU_ORDER → 送料暂收单 SL_RECV → 来料检验单 QC_INSP → 采购入库单 PURCHASE_IN / 暂收退回单 QC_RETURN**
> 状态:**P0 已实现并实测通过(2026-09-20)**;P1/P2 待排期
> 起因:一张采购订单会**分多批**送料暂收,而原来的生单逻辑是"整单生单 + 整行占用",既生不出第二张暂收单,也没有任何"批次"标识可追溯。
>
> **命名口径更新(2026-09-20)**:送料暂收单面板编码 `SL_RECV` **已改名为 `QC_RECV`**
> (空壳面板 QC_RECV 已删;物理表仍 `sl_recv`、单据前缀仍 `SL`)。
> 本文下文出现的 `SL_RECV` 一律即指现 `QC_RECV`,见 `docs/方案-来料三单对照参照库字段补充.md` §四。

---

## 〇、P0 实施记录(2026-09-20,已落地)

| 层 | 落地内容 |
|---|---|
| **DB** | `tools/migrate-batch-link.sql`:① 参数表 `yj_app_setting`(默认 `receive_over_ratio=0.05`);② 批次台账 `yj_doc_batch`(筛选唯一索引只对 ACTIVE 唯一);③ `form_flow_link.batch_no`;④ 暂收/检验/入库/退回 头+行 `批次号` 列 + `MS_Description`;⑤ `yj_field` 8 行 + `yj_translation` 批次号 ×10 语言。<br>`tools/migrate-batch-backfill.sql`:存量按「一张暂收单=一个批次」回填 + 沿链路下传 + 自检(实测 `YJ-20260915-08-001` 已贯通 SL-2026-09-0012 → IJ-2026-09-0011 → PI-2026-09-0020 / TH-2026-09-0002) |
| **后端** | 新 `BatchService`(取号可回收 / 台账 / 行级已送量 / **退货回冲** / 超送比例);`VoucherFlowService` 剩余量按行 = 订单量 − Σ有效批次 + Σ已审核退货,新增 `linkBatch`(按量占用 + 批次号),`release` 同时释放台账;`PushGenerateHandler` 新增 `batchLines` / `generateBatch`(分批生单,**来源自带批次号则继承**,否则取新号),推式按钮不再"整单拒绝";`PxController` 新增 `/px/batchFlow/lines`、`/px/batchFlow/generate`、`/px/batchFlow/batch`;`PanelConfigService` 头/行 `批次号` 同义词 + `selectConfig.batchFlow` + `metadata.pushTargets`;`ButtonService` 检验审核自动生单带批次号、作废释放台账 |
| **前端** | 新 `BatchSendDialog.vue` 分批送料对话框(行号/物料/订单数量/已送/已退回/剩余/**可送上限**/本次送料数量;底部自动批次号 + 超送比例 + 已有批次);`PanelxList` 拦截「生成XX」→ 批次目标面板弹该对话框;`SelectVoucherDialog` 对 `batchFlow` 目标走分批生单(本次数量 = 所选行剩余量);新词条 ×10 语言 |
| **验证** | `_verify-batch-p0-api.mjs`(分批生单/内容/剩余递减/超量按比例拒绝/删草稿序号回收+台账留痕/选单口径)、`_verify-batch-p0-chain.mjs`(四单批次号一致 + 四跳 link 带批次号 + 按批次反查 + 收尾清理)、`_verify-batch-p0-ui.mjs`(工具栏 → 分批对话框 → 填量生单 → 跳转目标面板 → 收尾作废)—— 全部 PASS |

**序号回收的落地口径**(与 §3.2 一致):释放即回池 —— 台账里该批次行置 `RELEASED` 留痕(不删),取号只认 `ACTIVE` 行,故下一批次优先补空洞;同一批次号历史上释放多次时会有多条 RELEASED 留痕行,`/px/batchFlow/batch` 反查取最近一条。

**仍未做(P1/P2)**:采购订单里的「送料批次」页签与逐批跳转、超送推金蝶策略(金蝶"不允许超额入库")、独立批次台账面板(可选)、批次追溯报表/扫码/批次号推 ERP。

### 0.1 补充(2026-09-20 第二轮,用户反馈后)

| 诉求 | 落地 |
|---|---|
| 表头也要带批次号 | **四张单(暂收/检验/入库/退回)三处都可见**:① 单据卡片头部(单据号右侧新增靛蓝「批次号」标签)② 表头字段区 ③ 明细表格列(界面实测四处值一致) |
| 下游单据表头+表格带批次号 | 同上,且左栏「单据选择」中间列新增「批次号」(暂收/检验/入库/退回四个面板) |
| 弹窗可设置超送比例 | 分批送料弹窗顶部「超送比例」改为**可编辑输入(0~100%)**,默认取系统参数(5%),**本次生效**:改动后各行「可送上限」即时重算(剩余 ×(1+比例)),提交时随请求带给后端 `overRatio`(后端夹 0~1 后再校验)。实测:系统 5% 时申请 15% 超送被拒;弹窗改 20% 后同一申请放行 |

### 0.2 送料批次页签(方案 B,2026-09-20 第三轮 —— **2026-09-21 已撤回**)

> 三次口径演进:①页签(方案 B,§0.2)→ ②独立台账面板(§0.3,已回退)→ ③**表头一行摘要 + 浮层(§0.4,现行)**。

> ⚠️ **本节记录的采购订单「送料批次」页签已在 2026-09-21 撤回**(用户口径:
> 「再做一个明细表太冗余了,采用方案 A」)。回收站口径:页签相关前端代码、页签专用词条、
> 对应探针全部移除;**同时发现的 `?docNo=` 跨面板定位 bug 修复保留**(与页签无关,见本节末)。

**为什么当时要在采购订单上加页签**:分批送料后,一张采购订单会对应**多张**送料暂收单(每批一张),
原来的「一条链路一行」看不到"这张单送了几批、批次号是什么、分别去了哪张暂收单",
现场只能凭单号猜。**方案 B = 采购订单明细区多一个「送料批次」页签**,只读窄表 + 点行跳转。
(方案 A = 独立批次台账面板,当时因"会让采购订单页面变复杂"被否掉;次日用户改判采用方案 A。)

| 项 | 落地口径 |
|---|---|
| 页签出现条件 | 面板 = 采购订单 且 **审批状态 ∈ {已通过, 已审批}** 且 单据状态 ≠ `已中止`。**不能只认「已审核」**——库里 200 张采购订单中 138 张已走到「已完成」,只认已审核会让它们全部丢掉这个页签(实测踩到) |
| 页签内容 | 批次号 / 日期 / 数量 / 状态(`有效`/`已释放`)/ 暂收单 / 操作[查看];底部一行汇总:`已送 N 批次数 · 已送合计 · 剩余 · 已退回(可补送)` |
| 数据来源 | 复用 `/px/batchFlow/lines`(台账 `yj_doc_batch` 的 ACTIVE 行 + 行级已送/剩余),不新增后端接口 |
| 点行跳转 | 「查看」→ 送料暂收单面板(`QC_RECV`)+ `?docNo=<暂收单号>` 精确定位 |
| 未送过料 | 页签仍显示,表内提示「该订单暂无送料批次」,汇总全 0 |

**性能(用户明确关注:纯前端会不会拖慢启动)**——不会,页签是**懒加载**的:

- 首屏**不发**批次请求:`openPanel` 打开采购订单后实测 `batchFlow/lines` 调用数 = **0**;
- 只有**点开该页签那一刻**才请求一次,同一张单据来回切换页签不重复请求(按单据号去重,换单/刷新后失效重取);
- 无轮询、无定时器、无额外首屏字段;**实测首屏可达耗时** 采购订单 4 张样本 559 / 917 / 949 / 953 ms,
  与不带该页签的送料暂收单(1020 ms)同级 —— 页签本身不产生可测的启动开销;
- 代价:产物 chunk 只增加页签模板/样式的几 KB(`PanelxList` 336 kB,`npm run build` 通过)。

**顺带修掉的一个真 bug(跨面板跳转落到"列表第一张")**:
`?docNo=` 原先只在 `onMounted` 消费,而采购订单→暂收单是**同一条路由记录换面板参数**,
组件被复用、`onMounted` 不再执行 → 跳过去停在列表首单(实测:点批次行跳 `SL-2026-09-0004`,
界面却显示 `SL-2026-09-0024`)。现抽出 `applyDocNoQuery()`(消费后从地址栏摘除该参数),
`onMounted`、面板切换 watcher、同面板换单 watcher 三处共用 → 实测跳转后卡片头部即为目标单据,
且「第 1/1 张」(仅该单)。

**i18n**:页签与表内 9 个新词条(送料批次 / 该订单暂无送料批次 / 批次数 / 已送合计 /
已退回(可补送) / 有效 / 已释放 / 暂收单 / 批次号)已按规范补进 `src/i18n/locales/*.js` 10 个语言文件。

**探针**:`tools/archive/_verify-batch-tab-ui.mjs`(随功能一并撤回)。

---

### 0.3 送料批次台账面板(方案 A,2026-09-21 上午落地 —— **当日下午已回退,见 §0.4**)

> ⚠️ **本节记录的独立台账面板已在 2026-09-21 下午回退**(用户口径:「再回退,方案 A(更轻):
> 连页签都不加,只在表头字段区「采购订单号」旁边加一小行只读摘要,点开浮层看批次窄表」)。
> 回退由 `tools/migrate-batch-ledger-drop.sql` 完成(面板行/字段行/视图/面板名译名四清);
> 现行实现见 §0.4。本节保留原实现记录,供将来真要做独立台账面板时复用(含全部踩坑)。

**用户口径(2026-09-21)**:「撤回这个实现,再做一个明细表太冗余了,采用方案 A」——
采购订单页面保持原样(不新增页签/明细表),批次台账做成**独立面板**。

| 项 | 落地 |
|---|---|
| 面板 | `BATCH_LEDGER` / **送料批次台账**,菜单 `采购管理 → 台账 → 送料批次台账` |
| 形态 | `mode=flat` + `category=报表` → 报表式**只读**列表(无 新增/保存/删除/生单 按钮,表格内无输入框) |
| 数据源 | 视图 `v_batch_ledger` = 批次台账 `yj_doc_batch` × 占用明细 `form_flow_link` × 采购订单行 `bl_pu_order`;**一行 = 一个批次的一条来源行** |
| 列(17) | 批次号 / 采购订单号 / 行号 / 物料编码 / 物料名称 / 规格型号 / 计量单位 / 订单数量 / 本批送料数量 / 批次数量 / 状态 / 去向单据 / 去向单号 / 送料时间 / 释放时间 / 制单人 / 备注 |
| 查询 | 7 个 query 位字段(批次号/采购订单号/物料编码/物料名称/状态/去向单号/送料时间)→ 查询弹窗逐项 `LIKE '%值%'` 模糊 |
| 留痕 | **释放批次也列出**(状态=已释放),台账可追溯"批次号回收后原本用在哪张单" |
| 排序/翻页 | `ORDER BY id DESC`(id 按送料时间升序编号)→ 最新批次在最上;`page_size=50` |
| 只读兜底 | 字段全 `editable=0` + `mode=flat`(后端不下发任何编辑按钮)+ 视图为多表 JOIN(物理不可写) |

**实测(2026-09-21,46 行台账数据)**:界面列头 17 列与 `yj_field.seq` 完全一致;
46 行全部渲染;有效 19 / 已释放 27 与视图一致;查询弹窗 7 字段,填「批次号=YJ-20260912-02」→ 界面 4 行 = 接口命中 4 行;
英文界面列头全部英文(Batch No. / Purchase Order Number / Seq / Material Code / …)。
探针:`tools/archive/_verify-batch-ledger-ui.mjs`(全部 PASS)。

**踩坑(已固化进脚本注释与自检)**:

1. **平表视图必须暴露 `id` + `asp_cancel`** —— `QueryService.queryFlat` 固定 `SELECT t.id AS __id … ORDER BY t.id DESC`
   且 `WHERE ISNULL(t.asp_cancel,'N')<>'Y'`;缺任一列直接 207/500(首次执行就踩到 `列名 'asp_cancel' 无效`)。
2. **`mode=flat` 与 `category=报表` 必须成对** —— `category` 是前端 `reportMode` 的唯一开关;只给 flat 不给报表,
   前端会落进「单据卡片」分支而 flat 面板没有 header 字段 → 空白页。
3. **占用明细 JOIN 必须带 `target_form_no`** —— 批次号在作废后**回收复用**(唯一索引只约束 ACTIVE),
   同一个 `batch_no` 会对应多张历史下游单;只按 batch_no 关联会把历史批次的行并进来(行数翻倍)。
   实测当前库该复合键无重复,视图仍按 (批次, 来源面板, 来源单, 目标单, 来源行) 聚合 `MAX(linked_quantity)` 防未来脏数据。
4. **批次状态用视图 `CASE` 翻中文** —— `dict_sql` 只影响查询弹窗下拉选项,报表单元格渲染的是原值
   (`{{ row[column.prop] }}`),英文界面下这类**数据值**仍是中文(与既有报表面板 批号追溯/工单齐套表 口径一致;
   面板名与列头走 `yj_translation` + 语言包,已全部翻译)。
5. **菜单不在数据库** —— 左侧菜单是前端 `business/menus.js` 静态表,插入 `yj_panel` 不会出现菜单项;
   admin 无需 `yj_role_panel` 授权行,非 admin 角色要补 `(role_id,'BATCH_LEDGER','view','N')`(现库该表为空、只有 admin)。
6. **`execute` 通道**:手工跑迁移必须 `sqlcmd -f 65001`,否则脚本被按 ANSI 读入 → 中文标识符全乱码、
   语句逐条失败(本次第一遍就是这样静默失败的);DbSync(Java)按 UTF-8 读,不受影响。

**保留项(与页签无关的真 bug 修复)**:`?docNo=` 原先只在 `onMounted` 消费,跨面板复用组件时跳转落到列表首单
(实测点批次行跳 `SL-2026-09-0004` 却显示 `SL-2026-09-0024`);现抽出 `applyDocNoQuery()`,
`onMounted`/面板切换/同面板换单三处共用 —— 该修复独立于页签,页签撤回时保留。

---

### 0.4 现行实现(2026-09-21 下午定稿):表头一行只读摘要 + 点击浮层

**用户口径**:「连页签都不加,只在表头字段区「采购订单号」旁边加一小行只读摘要:
`采购订单号: YJ-20260916-01    送料: 已送 2 批 / 剩余 5500 / 可补 20 ▸`;
点这行弹一个小浮层(批次窄表)。**页面结构一点不动,只多一行文字**;
缺点是信息被压缩成一行,批次多了要点开才看清。」

| 项 | 落地 |
|---|---|
| 位置 | 采购订单**表头字段区**内、**排在所有表头字段之后**(DOM 顺序即最后一项,不做任何 order 干预)—— 页面结构/字段数量/DOM 语义零改动;用户口径:「不用跟在单据编号后面,保证他出现在最后一个字段的后面,**并且表头修改后也是这个原则**」。因摘要行渲染在 `headerEditFields` 循环之后,表头字段怎么改(显隐/栏名/顺序/栏目设置)它都自动跟在最后 |
| 外观 | 靛蓝虚线胶囊(12px、圆角 13px、`▸` 展开标),与「已审批」角标同色系;`cursor:pointer`;hover 加深 |
| 文案 | `送料  已送 N 批 / 剩余 X / 可补 Y ▸`(N=台账有效批次数,X=行级剩余合计,Y=已退回可补合计) |
| 浮层落位 | 浮层挂在**表头字段区的末尾**(0 宽锚点独占一行,排在所有字段之后)→ 面板出现在**字段区下方、右缘对齐字段区右缘**,实测**不遮挡任何表头字段**(用户口径:「悬浮层最好放在表头字段的最后」) |
| 浮层内容 | 点击摘要行弹出 760px 浮层:标题 `送料批次 · <单据编号> · 已送合计 <Σ>`,窄表 6 列 = 批次号/日期/数量/状态/暂收单/操作[查看];空态提示「该订单暂无送料批次」 |
| 跳转 | 浮层「查看」→ 送料暂收单(`QC_RECV`)+ `?docNo=` 精确定位(沿用 §0.2 的 `applyDocNoQuery`) |
| 出现条件 | 面板 = 采购订单 且 **审批状态 ∈ {已通过, 已审批}** 且 单据状态 ≠ `已中止`(= §0.2 同一判据:不能只认「已审核」,库里 138 张「已完成」订单同样要看批次) |
| 数据来源 | 复用 `/px/batchFlow/lines`,**不新增后端接口、不新增面板/字段/视图**(§0.3 的面板对象已全部下线) |

**性能**(沿用上次口径,实测):

- **切换单据时请求一次**(按单号缓存,同一单据停留不重复请求——探针实测:换单 1 次、停留仍为 1 次);
- **点开浮层再强制刷新一次**(保证看到的是最新批次),不点不看 = 不产生额外请求;
- 非采购订单面板、未审批/已中止的采购订单:**一次请求都不发**;
- 无首屏阻塞:摘要行是纯前端渲染,数值到达后响应式回填。

**验证**(`tools/archive/_verify-batch-summary-ui.mjs`,全部 PASS):摘要数值与接口一致(已送批次数/剩余/可补);
点击弹浮层且窄表行数=台账有效批次数、列头 6 列、状态中文;「查看」跳转定位到目标暂收单;
已中止订单无摘要行;独立面板三处(后端配置/前端菜单树/数据库视图)确认已下线;
**浮层落位**4 条断言(在字段区下方/右缘对齐/未遮挡字段=0/摘要行是字段区最后一项);
**表头改动回归**:走真实表头调整接口隐藏一个字段 → 摘要行仍排在最后一个字段之后(可见字段 10→9),
验完原样还原(字段顺序 + alias/hidden/visible 逐项一致)。

**已知取舍**:信息压缩成一行,批次多时要展开浮层才看得全(用户已确认接受);
报表/浮层的单元格值是**数据值**(中文),英文界面下这类值不翻译(与既有报表面板口径一致,
面板名/字段名/按钮名走 `yj_translation` + 语言包,已全部翻译)。

---

## 一、一句话结论

把「整单生单 + 整行占用」改成「**按行分批生单 + 按批次数量占用**」:

1. 给暂收单及其下游单据加 **批次号**(送货批次,单据级,自动取号,如 `YJ-20260915-08-01`);
2. 新增批次台账 **`yj_doc_batch`**,记录每个批次送了多少、去的哪张下游单、是否释放;
3. `form_flow_link` 记录**每批次实际送料量**(现在是整行量),于是"剩余量 = 订单量 − Σ各批次量"可算;
4. 批次号沿 暂收→检验→入库/退回 **全程下传**,一个批次号可一次拉出四张单。

---

## 二、现状核对(2026-09-20 实测代码与库)

### 2.1 两条生单入口,语义不同

| 入口 | 位置 | 现状行为 |
|---|---|---|
| **推式**「生成送料暂收单」 | 采购订单单据内按钮 → `PushGenerateHandler` | **整单**映射订单全部明细行;**只要该订单已向 SL_RECV 生过一次就整单拒绝**("该单已向目标面板生单(或已选单占用),请先删除下游草稿后重试") |
| **拉式**「选单」 | 送料暂收单内按钮 → `SelectVoucherDialog` + `VoucherFlowService.sources` | 来源列表只列 **已审核** 且 **未完全占用** 的订单行,显示 `已生单数量/剩余数量`;**可只勾选部分明细行**;但生成时**整行数量照搬,不能改量** |

两条路径最后都调用 `VoucherFlowService.link(...)`,而它写入的是:

```java
double qty = qtyOf(src);           // 源行整行数量
... source_quantity = qty, linked_quantity = qty   // ← 送多少都记成"整行送完"
```

→ **部分送料在数据上等于整行送完,剩余量根本算不出来**;推式那条还被"整单仅一次"卡死。这就是你说的问题。

### 2.2 已有的正确范式(可以照搬)

检验 → 入库 / 退回 两跳**已经是行级按量占用**:

- `ButtonService.inspAutoPurchaseIn`:写入 `source_quantity = 数量`、`linked_quantity = 合格数量`(不良部分余量留给退回链);
- `ButtonService.inspAutoReturn`:写入 `linked_quantity = 不良数量`。

本方案就是把这两跳的语义**往前推到 采购订单 → 暂收**,链路口径统一。

### 2.3 现有"批次"相关字段(别混用)

| 位置 | 字段 | 性质 |
|---|---|---|
| `bl_pu_order`(采购订单行) | `批次号`、`商品是否开启批次` | 金蝶带过来的**物料批次**(行级) |
| `qc_insp_detail` / `bl_purchase_in` / `qc_return_detail` | `批号` | 物料存货批号(行级,库存/LOT_TRACE 用) |
| `bd_purchase_in` | `资金批次` | 金蝶同步的资金口径批次 |
| 暂收/检验/入库/退回 **表头** | **无** | ← 本次要补的就是"送货批次号" |

### 2.4 占用现状(库里真实数据)

| 链路 | link 行数 |
|---|---|
| PU_ORDER → SL_RECV | 28 |
| SL_RECV → QC_INSP | 27 |
| QC_INSP → PURCHASE_IN | 14 |
| QC_INSP → QC_RETURN | 3 |

且 `YJ-20260915-06` 一张订单已对应 **6 张**暂收单 —— 说明"分批送料"实际上**已经在发生**,但既没有批次标识,也没按量核减(每张下游单都把整行记成已占用),所以现在**算不出这张订单到底还剩多少可送**。

---

## 三、设计口径

### 3.1 两种"批次"严格分开

| 名称 | 含义 | 粒度 | 来源 | 用途 |
|---|---|---|---|---|
| **批次号**(新增) | 该采购订单的**第几次送料** | **单据级(表头)** + 行上冗余 | MES 生单时自动取号 | 分批追溯、按批对账、开票/索赔取证 |
| 批号(已有) | 物料的**存货批号** | 行级 | 金蝶/入库取号 | 库存批次、批号追溯 |

**结论**:`批次号` 放表头(一单一批次),同时**冗余写到明细行**(便于行级对账与后续推 ERP);**不复用** `批号`。

### 3.2 批次号编码(已定稿 2026-09-20)

```
<采购订单号>-<3位批次序号>      例:YJ-20260915-08-001 / YJ-20260915-08-002
```

- **序号可回收**(用户口径 2026-09-20):取号 = 该订单 `1..N` 中**第一个未被"有效批次"占用的序号**;
  删除下游**草稿**暂收单 → 该批次台账行随之删除、序号回到可用池,下一次生单优先补空洞。
- **"有效批次" = 暂收单还在的单据**:已审核/已作废的暂收单都仍占号(单据还在,批次号不能重复);
  只有"草稿被删除"(连带释放 `form_flow_link` 占用)才回收序号。已流转到检验/入库/退回的暂收单删不掉
  (现有守卫:必须先弃审 → 草稿 → 删除),所以回收范围天然安全。
- 释放痕迹不丢:`form_flow_link` 保留 `RELEASED` 行 + `release_time`(台账只记有效批次,历史查 link)。
- ⚠️ 复用风险:若单据**已打印/外发给供应商**后再删除,新批次可能拿到同一个号 → 追溯容易混。
  建议流程上约束"打印过的批次不要删"(系统不硬拦)。
- 超长单号:订单号超过 40 字符时截断拼接(批次号最大长度 50)。

### 3.3 数量口径与**超送**(超送口径已定稿 2026-09-20)

- **可送量(按行)** = `订单行.数量` − Σ(该行所有**有效批次**的送料数量) `+ 允许超送额度`;
- **允许超送**:是。额度 = `剩余量 × 超送比例`,比例**可配**:
  - 系统级默认值(面板/系统参数,建议初始 `5%`,可改 0 关闭);
  - 生成暂收单时可在对话框里**按单覆盖**(默认取系统值,权限内可改小/改大);
  - 校验:`0 < 本次送料数量 ≤ 剩余量 × (1 + 比例)`;超出提示"超出允许超送比例"。
- 数量比较**限同单位**:订单行 `单位/计量单位` ↔ 暂收行 `计量单位`,不一致直接拒绝并提示(不做跨单位换算);
- ⚠️ **超送与金蝶的已知冲突(实测)**:推 ERP 时若入库数量 > 订单剩余,金蝶返回「**不允许超额入库**」(本项目 2026-09-20 实测过)。因此超送部分在推送时需要二选一:
  (a) 超送行**不带源单关联**推送(金蝶侧作为无源单入库);
  (b) 或金蝶侧开启超收容差后再带源单推。
  → 归属到 P1,本方案 P0 只在 MES 内允许超送并如实记录,推送策略单独定。
- 只对**已审核**订单可生单(现有守卫保留),已中止/作废/审批中一律不可(现有状态机已具备)。

### 3.4 占用与释放

- **生成**:`form_flow_link` 增列 `batch_no`;每行写
  `source_quantity = 订单行数量`、`linked_quantity = 本次送料数量`、`batch_no = 批次号`;
- **校验**:去掉"整单已生单就拒绝",改为**按行剩余**判断 → 有剩余就能再生一批;
- **释放**:删除下游**草稿** → 该批次 link 置 `RELEASED`(机制已有);下游已**审核**则必须先弃审/作废(沿用现有守卫);
- **台账**:`yj_doc_batch.status` = `ACTIVE / RELEASED / CLOSED`;订单所有行剩余为 0 时可标记该订单"送料完成"。

---

## 四、数据模型(改动清单)

1. **新表 `yj_doc_batch`(批次台账,含中文注明 + 索引)**

   | 列 | 说明 |
   |---|---|
   | `id` | 主键 |
   | `source_panel_code` / `source_form_no` | 来源(采购订单)面板 + 单号 |
   | `batch_seq` | 该订单内批次序号(1,2,3…) |
   | `batch_no` | 批次号(编码规则见 §3.2) |
   | `batch_qty` | 本批次送料数量合计 |
   | `status` | ACTIVE / RELEASED / CLOSED |
   | `target_panel_code` / `target_form_no` | 生成的下游单据(暂收单) |
   | `create_by` / `create_time` / `release_time` / `remark` | 留痕 |

   索引:唯一 `(source_panel_code, source_form_no, batch_seq)`;普通 `(batch_no)`、`(target_panel_code, target_form_no)`。

2. **`form_flow_link` 加列** `batch_no nvarchar(50) NULL` + 索引 `(target_panel_code, target_form_no)`。

3. **单据表加列 `批次号 nvarchar(50)`(头 + 行各一,8 处,均写 `MS_Description`)**
   `sl_recv` / `sl_recv_detail`、`qc_insp` / `qc_insp_detail`、`bd_purchase_in` / `bl_purchase_in`、`qc_return` / `qc_return_detail`。

4. **元数据与译名**:上述 8 处各注册 `yj_field.批次号`(头 `place=query,header`;行 `place=detail`);`yj_translation` 补 `批次号` × 10 语言。

5. **`PanelConfigService`**
   - `FLOW_HEAD_SYNONYMS` 增 4 条直通:`PU_ORDER|SL_RECV`、`SL_RECV|QC_INSP`、`QC_INSP|PURCHASE_IN`、`QC_INSP|QC_RETURN` 的 `批次号`;
   - `FLOW_DETAIL_SYNONYMS` 增行级 `批次号` 直通(与现有 `行号 → 采购订单行号` 同一机制);
   - 新增链路"数量字段对"配置(来源数量列 → 目标数量列),供可送量计算复用。

---

## 五、生单流程(改造后)

### 5.1 推式:采购订单 →「生成送料暂收单」

1. 点按钮 → 弹**分批送料对话框**(不再直接生成):
   表格列 = `行号 / 物料编码 / 物料名称 / 规格型号 / 订单数量 / 已送 / 剩余 / 本次送料数量(可编辑,默认剩余)`;
   底部 = 合计数量、**批次号(自动生成,只读)**、供应商、单据日期。
2. 校验:勾选行 `0 < 本次 ≤ 剩余`;全为 0 时"确定"禁用;单位不一致行标红。
3. 生成:创建 SL_RECV 草稿
   - 头:供应商 / 供应商代码 / 采购订单号 / **批次号** / 日期(创建当日,沿用现有口径);
   - 行:物料 / **数量 = 本次送料数量** / 单价 / 计量单位 / 采购订单行号 / **批次号**;
   写 `form_flow_link`(带 `batch_no` 与本次数量)+ `yj_doc_batch`,跳转暂收单继续填写。
4. **同订单可反复生单**,直到所有行剩余为 0 → 按钮置灰提示"已全部送料"。

### 5.2 拉式:送料暂收单 →「选单」

- 来源列表语义不变(仅已审核、仅还有剩余行),把 `可生单数量` 明确为**剩余数量合计**;
- 生成对话框加"本次送料数量"列(默认剩余),与推式**共用同一套校验与生成器**;
- **批次号自动取号**(无需用户输入);
- 一次勾选多张订单时**仍一单一张暂收单**(保持现状;跨订单合并见 §七 问题 3)。

### 5.3 下游自动链路(已存在,只补批次号)

| 跳 | 触发 | 补什么 |
|---|---|---|
| SL_RECV → QC_INSP | 暂收单「生成来料检验单」/ 审核钩子 | 头 `批次号` ← 暂收单;行 `批次号` + `采购订单行号` |
| QC_RECV → PURCHASE_IN | 暂收单「生成采购入库单」(2026-09-22 新增) | 头 `批次号`/`批次键` ← 暂收单;行 `批次号` + `采购订单行号`。**采购订单的免检直达出口已取消**,暂收单判为免检时走这一跳 |
| QC_INSP → PURCHASE_IN | 检验单审核(合格数量>0 的行) | 头/行 `批次号`(现有 `inspAutoPurchaseIn` 里补一行) |
| QC_INSP → QC_RETURN | 检验单审核(不良数量>0 的行) | 头/行 `批次号`(现有 `inspAutoReturn` 里补一行) |

→ **同一批次的"入库 + 退回"共用同一批次号** —— 按批次号一次拉出 暂收/检验/入库/退回 四张单,这是本方案最大的价值。

### 5.4 界面呈现

- 暂收/检验/入库/退回 单据头部显示 `批次号`(自动带出,只读);**查询条件加 `批次号`**(支持模糊搜全库,与现有 keyword 一致);
- 采购订单单据界面加「送料批次」页签或左上角汇总:`已送 N 批 / 剩余数量 x`,台账行可点击跳到对应暂收单;
- 左栏「单据选择」中间列:暂收单那一栏可把 `采购订单号` 换成/加上 `批次号`(更直观,待你定)。

---

## 六、改动清单与工作量评估(仅评估,不含实现)

| 层 | 改动 | 预估 |
|---|---|---|
| DB | 2 支迁移(结构:`yj_doc_batch` + `form_flow_link.batch_no` + 8 列;数据:存量回填 + 元数据 + 译名) | 0.5 天 |
| 后端 | `PushGenerateHandler` 改分批、`VoucherFlowService.sources/link` 按量与批次、新 `BatchService` 取号、`inspAuto*` 补批次号 | 1.5 天 |
| 前端 | 分批送料对话框(推式/拉式共用)、订单批次汇总、左栏列调整 | 1.5 天 |
| 验证 | 探针:3 批送料 / 超量拒绝 / 删草稿回滚(序号回收) / 批次贯通四单 / 按批次反查 / 退货回冲 | 0.5 天 |
| **合计** | | **≈ 4 天** |

---

## 七、决策记录

### 7.1 已定稿(2026-09-20 用户确认)

| # | 事项 | 结论 |
|---|---|---|
| 1 | 批次号编码与取号 | **`<采购订单号>-<3位序号>`**(如 `YJ-20260915-08-001`);**序号可回收** —— 取最小未占用序号,删除草稿序号回到可用池(详见 §3.2) |
| 2 | 超送 | **允许**,额度 = 剩余量 × **可配置比例**(系统默认值 + 生成时按单覆盖);超送与金蝶"不允许超额入库"的冲突见 §3.3,推送策略放 P1 |
| 3 | 一个暂收单装几张采购订单 | **一张暂收单 = 一张采购订单**(保持现状,批次号放表头;行上仍冗余写一份,将来要合并只放开入口) |
| 4 | 退货是否回冲已送量 | **回冲**:退回单审核后把 `退货数量` 加回该行可补送额度;**只影响可送量**(送货量/批次记录/退回单本身不变) |
| 5 | 批次台账要不要独立面板 | **先不做面板**:只建数据表 `yj_doc_batch`;批次信息放进「采购订单」单据里的**「送料批次」页签/汇总**(零新增菜单与权限)。将来要独立面板再加(见 §7.2)

### 7.3 修订(2026-09-21,方案 B → 方案 A)

| # | 事项 | 结论 |
|---|---|---|
| 5′ | 批次台账的呈现方式 | **改判为独立面板(方案 A)**。用户口径:「撤回这个实现,再做一个明细表太冗余了,采用方案 A」——采购订单页面**不加页签/明细表**,保留原样;台账做成独立只读面板 `BATCH_LEDGER`(菜单 采购管理 → 台账),实现与踩坑见 §0.3 |
| 5″ | 撤回范围 | 采购订单「送料批次」页签的前端代码、页签专用词条(送料批次/该订单暂无送料批次/批次数/已送合计/已退回(可补送)/暂收单)、对应探针全部移除;`?docNo=` 跨面板定位修复**保留**(与页签无关的真 bug) |
| 5‴ | 原「页签只在有批次时出现」的性能顾虑 | 随页签一并消失:独立面板的数据只在**打开该面板时**查询一次,采购订单首屏零影响(实测首屏可达耗时 559~953 ms,与其它面板同级) | |

### 7.2 决策依据(备查)

**问题 3(一单多订单)为什么最终选"一单一订单"**

- 现在的行为:一次选单勾多张订单 → 每张订单各生成**一张**暂收单;推式生单也是一订单一张。
- "能合并"的代价:一张暂收单里装多张订单的行时,**表头批次号失去唯一语义**(一行属于不同订单的不同批次),
  必须整体改为行级口径,入口、列表、台账、追溯都要跟着改(+约 1 天)。
- 结论:按"一单一送"操作即可;**行上照样写批次号**,将来真需要"一车一单"只改入口,不动数据模型。

**问题 4(退货回冲)为什么选"回冲"**

- 场景:订单行 100 → 暂收 100(批次 `-001`)→ 检验判 20 不良 → 退回 20、入库 80。
- 不回冲:该行"已送 100 / 剩余 0",供应商换货补送 20 时**没有额度**(只能靠超送比例硬塞或人工改订单量)。
- 回冲:该行"已送 100 / 可补 20",补货按正常分批流程再生一批(`-002`);最终不补货就用现有**中止/关闭**收口。
- 口径:`可送量 = 订单量 − Σ批次送料量 + Σ退货数量`。

**问题 5(台账面板)为什么先不做**

- 表 `yj_doc_batch` **必须**(取号、按量占用、释放、追溯都靠它),但它是纯数据层,**不是面板**:
  菜单里不会出现,也不占权限配置。
- 做成独立面板要额外一整套元数据:`yj_panel` 注册 + `yj_field` + `yj_translation` 译名 + `yj_role_panel` 授权(约 +1 天)。
- 先用「采购订单」里的送料批次页签满足 95% 的查看需求;要独立台账面板随时可加(建议与 P1 批次追溯报表一起)。

---

## 八、存量数据兼容(迁移策略)

- 现网已有 28 条 `PU_ORDER→SL_RECV` 占用,无批次号 → 迁移脚本按"**每张暂收单 = 一个批次**"回填:
  `批次号 = 采购订单号-序号`(按暂收单创建时间排序取序号),同时写 `yj_doc_batch`;`linked_quantity` **保持原值**(存量本来就是整行占用);
- 回填后立即自检:**每张订单 Σ批次数量 ≤ 订单数量**,超出者输出清单人工核对(存量里已有手工造的数据,可能不平);
- 旧单据不阻断:没有批次号的存量单照常查看/审核,只是不参与按批次追溯。

---

## 九、验收标准(实现后按此写探针)

1. 一张订单 3 行 × 各 100,分 3 批各送 100 → 生成 3 张暂收单,批次号 `-001/-002/-003`;第 4 次生单被拒并提示"该订单已无剩余可送"。
2. 删除第 2 批草稿 → 该行剩余回到 200;台账中 `-002` 行删除、序号**回收**,重送生成 **`-002`**(不是 004);`form_flow_link` 里旧 `-002` 状态为 `RELEASED`(留痕)。
3. 每一批的 暂收 → 检验 → 入库 / 退回,四张单 `批次号` 完全一致;按批次号反查能列出四张单。
4. 同一订单两行部分送:剩余**按行**独立核减;超量超过比例 / 负数 / 单位不一致被拒。
5. 检验退回 20 件后,该行"可补送额度" +20(回冲口径);补送 20 走正常分批流程生成 `-003`。
6. 采购订单界面能显示「已送批次数 / 剩余数量 / 可补送额度」。

---

## 十、分期(P0/P1/P2 是**本方案的施工顺序**,不是另外的方案)

> `P0` = **最小可用闭环**:做完 P0,"一张采购订单分多批送料"这件事就能真正跑通(能生第二批、剩余量算得准、批次号能追)。
> `P1`/`P2` 是**同一个方案**的体验增强与外围(台账页签、金蝶推送策略、报表),不做也不影响 P0 的正确性。

| 期 | 内容 | 说明 |
|---|---|---|
| **P0(核心,先做这块)** | ① DB:新表 `yj_doc_batch` + `form_flow_link.batch_no` + 4 张单头/行 `批次号` 列 + 元数据 + 译名 + 存量回填自检<br>② 后端:批次取号(序号**可回收**)、`form_flow_link` 记**本次实际送料量**、剩余量按行核算(**含退货回冲公式**)、`PushGenerateHandler` 改分批、`inspAuto*` 补批次号<br>③ 前端:**分批送料对话框**(推式「生成送料暂收单」与拉式「选单」共用;含本次数量 + 超送比例校验)<br>④ 探针:3 批送料 / 超量按比例拒绝 / 删草稿序号回收 / 四单批次贯通 / 按批次反查 / 退货回冲 | 约 **4 天**;退货回冲只是可送量公式里的一个 +项,和剩余量计算同一处改动,故并入 P0 |
| **P1(体验与外围)** | ~~采购订单「送料批次」页签~~(2026-09-21 撤回)、**采购订单表头「送料」摘要行 + 点击浮层窄表**(2026-09-21 落地,见 §0.4)、左栏「单据选择」列调整、**超送推金蝶策略**(超送行不带源单推 / 或金蝶开超收容差) | 缺了不影响 P0 正确性 |
| **P2(追溯与集成)** | 按批次追溯报表(批次 → 暂收/检验/入库/退回 → 批号 → 库存)、扫码/二维码登记联动、批次号推 ERP | 锦上添花 |
