# 研发管理 · 面板 × 设计文件 逐面对照与改动差异

> 对照源:`C:\Users\x1787\OneDrive\Desktop\产品开发\产品开发`
> 现状真源:HSDZ_MES 库 `yj_panel` / `yj_field`(22 面 729 个字段行)+ `frontend/src/core/views/recordSheetConfigs.js`
> 会话:2026-09-18 grill 会话。口径决策见 §6。

---

## 0. 一页结论

**研发管理共 22 个面板**(`yj_panel` where `module_group='研发管理'` 实测):

| 组 | 面板数 | 面板 |
|---|---|---|
| 项目管理 | 3 | 立项申请 RD_APPROVAL / 项目实施计划 RD_PLAN / 项目进度查询 RD_PROGRESS |
| 测试记录·数据记录表 | 8 | 功能性滤效 / 碱性 / 矿化 / 抑菌 / 阻垢性能 / RO保护 / 浸泡安全 / 压降精度 |
| 测试记录·实验室使用记录表 | 4 | 加标水配置 / 内部委托测试申请 / 设备使用登记 / 仪器使用记录 |
| 产品文件 | 7 | 产品信息表 / 成型工艺清单 / 成型配方(已并入页 2) / 组装工艺清单 / 组装BOM表(已并入页 2) / 规格书 / 出货检验计划表 |

设计侧 25 份文件全部对照后:**12 面已一致**(只标注"无改动"),**7 面需改**,**2 面为新增**。

| 结论 | 面板 |
|---|---|
| ✅ 已一致,不动 | 立项申请 · 项目实施计划 · 数据记录表 8 张 · 规格书(仅封面补 2) · 设备使用登记表 · 仪器使用记录表 · 共享文件库 |
| 🔧 需改 | **项目进度查询**(18 列重构) · **产品信息表**(3 新字段 + 4 label) · **组装工艺清单**(2 新字段 + 变更履历 2 列) · **出货检验计划表**(表头 2 + 明细 2) · **规格书**(封面 2 字段) · **成型工艺清单**(label/提示语) · **成型配方页**(派生字段,待定) |
| ➕ 新增 | **产品文件列表**(设计《产品文件列表》页) · **样品编号表**(设计《产品开发样品编号》页) |

---

## 1. 对照总表(设计文件 → 面板)

| # | 设计文件 | 对应面板 | 结论 |
|---|---|---|---|
| 1 | `1.产品开发\立项申请表.xlsx` | RD_APPROVAL | ✅ 一致(14 字段逐项对上) |
| 2 | `1.产品开发\项目实施计划.xlsx` | RD_PLAN | ✅ 一致(8 行 + 测试方案 3 子项) |
| 3 | `1.产品开发\二三级四级项目控制表2026.xlsx` | RD_PROGRESS | 🔧 **大改** |
| 4 | `1.产品开发\数据记录表.xlsx`(7 sheet) | 碱性/矿化/阻垢/RO保护/浸泡安全/压降精度 | ✅ 一致 |
| 5 | 同上 `功能性滤效` sheet | RD_FILTER_EFF | ✅ 一致(动态样品列已实现) |
| 6 | `2.产品文件\1.产品信息表\产品信息表内容.xlsx` | RD_PROD_INFO | 🔧 **需改** |
| 7 | `2.产品文件\1.产品信息表\*.png` | —(参考截图,非设计) | ⚪ 金蝶云·星辰商品录入界面参考,见 §2.5 |
| 8 | `2.产品文件\2.成型工艺文件系统需求框架-v0.1\系统设计需求0903.docx` | RD_MOLD_PROC | ✅ 需求正文,字段已覆盖 |
| 9 | `…\20267月22日-最新烧结配方模板-1.xlsx` | RD_MOLD_PROC / RD_MOLD_FORMULA | 🔧 配方计算器页待定(§5.2) |
| 10 | `2.产品文件\2.组装\关键控制清单--标准库.xlsx` | RD_ASM_PROC | 🔧 **需改**(4 变体) |
| 11 | `2.产品文件\2.组装\组装工艺控制.xlsx` | RD_ASM_PROC 页 2 + 变更履历 | 🔧 **需改** |
| 12 | `2.产品文件\3.检验计划表\出货检验项目控制计划.xlsx` | RD_INSP_PLAN | 🔧 **需改** |
| 13 | `2.产品文件\2.1规格书\规格书细分.xlsx` | RD_SPEC_DOC | 🔧 封面补 2 字段 |
| 14 | `2.产品文件\2.1规格书\测试项目汇总.xlsx` | RD_SPEC_DOC 标准库 `spec.test` | ✅ 一致(26 组已入库) |
| 15 | `2.产品文件\2.1规格书\规格书示例\*.docx`(10) | RD_SPEC_DOC | ✅ 一致(章节库种子源) |
| 16 | `2.产品文件\产品开发系统需求汇总.xlsx` | 流程上位文档 + **产品文件列表** | ➕ **新增面板** |
| 17 | `2.产品文件\共享文件项目.xlsx` | RD_SHARE_FILE | ✅ 一致(3 类 6 子类) |
| 18 | `2.产品文件\2.成型工艺…\系统样品模板.xlsx` | 无(制造/销售域) | ⚪ 见 §5.8 |
| 19 | `3.实验室使用记录表\加标水配置记录表.xlsx` | RD_SPIKE_WATER | ✅ 一致(3 变体) |
| 20 | `3.实验室使用记录表\测试申请单.xlsx` | RD_DOM_TEST | ✅ 一致(内部/外部 2 变体) |
| 21 | `3.实验室使用记录表\设备使用登记表.xlsx` | RD_EQUIP_USE | ✅ 一致 |
| 22 | `3.实验室使用记录表\仪器使用记录表.xls` | RD_INSTR_USE | ✅ 一致 |
| 23 | `二三级四级项目控制表2026.xlsx` → `产品开发样品编号` sheet | — | ➕ **新增面板** |

---

## 2. 逐面差异明细

### 2.1 🟢 RD_APPROVAL 立项申请 —— 无改动

设计:公司名 + 右上 `YJ-XS002` + 文件管理人/密级/文件使用范围 + 八节(客户名/立项背景/机型及应用位置/滤芯炭棒规格或结构/项目开发目标/项目输出/开发周期要求/其它要求)+ 申请立项人/日期。

14 个设计字段与现有 `yj_field` header 逐项命中(含 `滤芯炭棒规格或结构` 的 label `滤芯/炭棒规格或结构`、右上角 `文档编号`)。

**改动:无。**

---

### 2.2 🟢 RD_PLAN 项目实施计划 —— 无改动

设计 8 行:项目名称 / 项目定级 / 测试内容 / 测试产品打样要求 / 测试目标 / 测试方案(测试条件·测试方法·测试标准)/ 测试计划 / 负责人+编制日期。

现有 header 全部命中(seq 30~100 + 120 负责人 + 130 编制日期);`测试计划` 由 `阶段1..10` 五字段 × 10 个阶段框承载。

**改动:无。**

---

### 2.3 🔧 RD_PROGRESS 项目进度查询 —— 全面重构(本次最大改动)

设计源:`二三级四级项目控制表2026.xlsx` → sheet **`产品开发项目（二三级）2026`**

```
标题行 B2:O3 (跨 14 列合并) = 产品开发二三四级项目控制列表
P2/Q2 = 密级 / 绝密          P3/Q3 = 适用范围 / 工程技术中心
B4:S4 合并 = 项目定级原则说明
  1.二级项目-开发性项目
  2.三级项目-A级或B级客户/近期（一年内）可能有重要经济效益
  3.四级项目-简单应对（检测/打样）
```

**明细 18 列**(`B5:S5` 共 18 个表头格;B/C 纵向合并表示 项目定级 是行组键):

| # | 设计列 | 建议数据键 | 类型 | 现状 |
|---|---|---|---|---|
| 1 | 项目定级 | `项目定级` | 下拉(二级/三级/四级) | ⚠ 现有 `项目层级`(label 项目(一/二级)),口径不一致 |
| 2 | 项目名称 | `项目名称` | 参照 RD_PLAN.项目名称 | ✅ |
| 3 | 子项目/尺寸 | `子项目/尺寸` | 文本 | ✅ |
| 4 | 项目编号 | `项目编号` | 文本 | ⚠ 现落 `说明` |
| 5 | 开发复杂度 | `开发复杂度` | 文本 | ❌ **缺** |
| 6 | 重要程度 | `重要程度` | 文本 | ❌ **缺** |
| 7 | 紧急程度 | `紧急程度` | 文本 | ❌ **缺** |
| 8 | 内容 | `内容` | 多行 | ✅ |
| 9 | 项目发起人 | `项目发起人` | 文本 | ⚠ 现落 `项目级`(键名荒谬) |
| 10 | 项目负责人 | `项目负责人` | 文本 | ⚠ 现落 `项目负责` |
| 11 | 立项日期 | `立项日期` | 日期 | ⚠ 现落 `实施进度` |
| 12 | 预计完成日期 | `预计完成日期` | 日期 | ⚠ 现落 `里程完成` |
| 13 | 项目定及变更 | `项目定及变更` | 多行 | ❌ **缺** |
| 14 | 状态 | `状态` | 派生只读 | ✅(派生快照已有) |
| 15 | 测试情况 | `测试情况` | 文本 | ⚠ 现落 `测试员` |
| 16 | 技术目标达成 | `技术目标达成` | 下拉(是/否/已达成) | ❌ **未落库**(`pendingAlign`) |
| 17 | 是否市场转化 | `是否市场转化` | 下拉(是/否) | ❌ **未落库** |
| 18 | 未转换原因 | `未转换原因` | 文本 | ❌ **未落库** |

**派生列说明**:第 14 项 `状态` **不是设计原表列** —— 设计原表的 `O 列(状态)` 是**手填的业务状态说明**(如 `53暂停/5A完成后置转5A+优矿配方`),语义与既有派生状态(`未开始 0/5 · 进行中 2/5 · 逾期 1 · 全部完成 5/5 · 无实施计划`)完全不同。因此:新增 `项目定及变更` 承接手填原意,`状态` 继续做派生只读列。

**现有错位是历史缺陷,不是设计差异**:`progressColumns.js` 里 3 列标了 `pendingAlign: true`(只显示不落库),另有 6 列把显示名映射到语义无关的物理列(立项日期↔实施进度、测试情况↔测试员、项目编号↔说明…)。本次一并修掉。

**其余待保留列**(数据已在库、不进控制列表):`说明` `谁来批准` `谁来检验` `未批准原因`。

**报告头**:设计标题跨 14 列、右上是密级/适用范围(不是单据日期)。现有 header 有 `密级`/`文件使用范围`/`文档编号`(隐藏)/`备注` —— 建议把 `文档编号` 取消隐藏显示在标题右侧。

---

### 2.4 🟢 数据记录表 8 张 —— 无改动

《数据记录表.xlsx》7 sheet + 功能性滤效独立实现,逐列对照:

| sheet | 面板 | 结论 |
|---|---|---|
| 功能性滤效 | RD_FILTER_EFF | ✅ 含动态样品列(2~6)、加标用原水水质条件条、4 数据结论 |
| 碱性 | RD_ALKALINE | ✅ 22 header + 12 detail 全中(浸泡 24H 口感 + 离子分析 钠/镁/钾/钙) |
| 矿化 | RD_MINERAL | ✅ 4 指标块(锶/偏硅酸/PH/TDS)× 3 取样方式 |
| 阻垢性能 | RD_SCALE | ✅ 3 样特殊配方 → 出水硬度1/2/3 + 阻垢率1/2/3 |
| RO保护 | RD_RO_PROTECT | ✅ 表头两段全部对上;`测试背景/目的` `项目名称` 等在设计原表也是空标签行,不补 |
| 浸泡安全 | RD_SOAK | ✅ 17 项卫生项目预填 + 3 尺寸列(需求2 30\*10\*113 / 需求2 35\*13\*107 / 需求4 40.5\*10\*114) |
| 压降、精度 | RD_DROP_PREC | ✅ 11 列(含前压/后压/压差三段 + 颗粒物去除率) |

**改动:无。**

---

### 2.5 🔧 RD_PROD_INFO 产品信息表 —— 3 新字段 + 4 label

设计源:`产品信息表内容.xlsx` sheet1 —— `B5:B17` 填写说明(12 条)+ `B21:E37` 正式表。

```
报告头: 惠州市银嘉环保科技有限公司 | 编号：
       产品信息表 | 编辑人 / 日期
一、产品基本信息
  产品编号 | 客户项目名称
  客户料号 | 产品管控等级
  产品功能类别 | 产品形态
二、规格与尺寸
  产品整体尺寸 | 炭棒尺寸
三、特殊性能描述
  主要性能描述
四、文件与签署
  客户图纸或规格书
  审核人（一级审核） | 审核人（二级审核）
  产品负责人
  备注
```

| 设计字段 | 现有 col_name | 结论 |
|---|---|---|
| 产品编号 | 产品编号 | ✅ |
| **客户项目名称** | — | ❌ **新增**(设计 `C7`,示例 `伊可普需求3`) |
| 客户料号 | 客户料号 | ✅ |
| **产品管控等级** | — | ❌ **新增**(设计 `C9` = 重点产品/KPC产品/其他) |
| **产品功能类别** | — | ❌ **新增**(设计 `C10` = 除余氯/VOC/重金属…) |
| 产品形态 | 产品形态 | ✅ |
| 产品整体尺寸 | 产品整体尺寸 | ✅ |
| 炭棒尺寸 | 炭棒尺寸 | ✅ |
| 主要性能描述 | 特殊性能描述 | ⚠ label 改为 `主要性能描述` |
| 客户图纸或规格书 | 客户图纸或规格书 | ✅(附件字段已实现) |
| 审核人（一级审核）/（二级审核） | 审核人(单格) | ⚠ 设计拆两级 → 增 2 格 |
| 产品负责人 | 责任人 | ⚠ label 改为 `产品负责人` |
| 备注 | 备注 | ✅ |
| **编辑人** | — | ❌ 报告头右侧签名格(设计 `D22`) |
| 编号： | 单据编号 | ✅(即报告头 `E21` 的"编号：") |

**现有独有、设计中无**:`下单数量` `单据日期`(保留不进纸面)。

### ⚠ 三分类字段的真实映射(实测现有字典后修正)

设计**不是**引入了新分类维度,而是把两个既有字段改了名 + 给 `产品类别` 换了一套更细的字典:

| 现有字段 | 现有字典取值 | 实际语义 | 与设计的关系 |
|---|---|---|---|
| `产品类别` | 阻垢 / 除铅 / 除余氯 / VOC / 抑菌 / 矿化 / 碱性(7 项) | **产品功能** | = 设计的 `产品功能类别` |
| `产品分类` | 重点产品 / KPC产品 / 其他(3 项) | **管控等级** | = 设计的 `产品管控等级`(取值完全相同) |
| `产品类型` | 成品 / 半成品 / 裸棒(3 项) | 形态 | 与设计 `产品形态` 语义重叠、取值不同 |
| `产品形态` | 包布 / 套网 / 打端盖 / 套PP棉 | 形态 | = 设计的 `产品形态` ✅ |

**处置**:新增 `产品功能类别` ← 搬 `产品类别` 历史值;新增 `产品管控等级` ← 搬 `产品分类` 历史值;`产品类别` 换字典为设计 20 项(`普通炭棒 / 低精度 / 高精度 / 重力式 / 氯胺 / 阻垢 / 碱性 / 矿化 / 矿化碱性 / 除铅 / 除重金属 / 除VOC / 多功能 / 抑菌杀菌 / 抑菌阻垢 / 抑菌矿化 / 载银 / 口感 / 烧结阻垢棒 / 烧结矿化棒`);`产品分类`/`产品类型` 退化为历史列(`hidden=1, visible=1` —— 列表不出现、表单仍可复核)。**只加列、不改既有列语义、不丢历史值。**

**填写说明 12 条**(设计 `B5:B17`)是设计文档的一部分、不是表字段 —— 建议做成各字段的 `placeholder`(已有同类先例:密度管控要求提示语「密度范围:~」)。

⚪ **`a89287605f87fda725b00baaf68e255.png` 是金蝶云·星辰的「商品录入」界面截图**(页签:基本信息/商品特性/商品条码/价格设置/库存预警/财务信息/其他信息;字段含 `*商品编码` `*商品名称` `商品类别` `*商品类型` `规格型号` `*计量单位` `物料编码` `产品整体规格L` `炭棒ODxDxL` `包装数` `物料属性` `批次管理` 等)。它是**参考实现截图,不是产品信息表的设计**(设计在 `产品信息表内容.xlsx`)。价值在于印证既有选型:本项目的 `产品编号`=商品编码、`产品名称`=商品名称、`产品整体尺寸`≈产品整体规格L、`炭棒尺寸`=炭棒ODxDxL。**不按它改字段。**

---

### 2.6 🟢 RD_MOLD_PROC 成型工艺清单(页 1)—— label / 提示语

设计源:`系统设计需求0903.docx`(需求正文)+ `最新烧结配方模板-1.xlsx` sheet `产品工艺管控`。

设计结构:
```
标题: 炭棒工艺管控清单 | 表单管理人 刘磊 / 密级 保密 / 使用范围 工艺科-成型车间 / 版本号
产品基本信息: 产品编号 | 产品名称 | 炭棒规格 | 产品管控类型 | 外观要求 | 生产车间
工序管控要求:
  灌料: 理论最低灌料重量g | 理论灌料中间值g | 理论最高灌料重量g | 理论水分 | 实际灌料重量计算公式
  烧结: 烧结炉参数 | 烧结时间/调速器参数
  热压: 热压要求
  冷却: 冷却参数设置
  脱模: 长度要求(最短/中间值/最长 mm) | 重量要求(最低/中间值/最高 g)
检验要求:
  炭棒尺寸: 外径mm | 内径mm | 内孔要求
  密度管控: 管控要求(密度范围：~) | 实际密度管控下限 | 实际密度管控上限
  跌落强度: 高度cm | 跌落次数 | 要求
  抗压强度: 测试间距mm | 压头下降速度mm/min | 强度要求kgf
  压降: 测试管路 | 测试流速L/min | 压降标准kpa
```

**逐项对照结论:设计列现有全部命中**(理论灌料三值 / 理论水分 / 实际灌料重量计算公式 / 烧结炉参数 / 烧结时间调速器参数 / 热压要求 / 冷却参数设置 / 最短·中间值·最长长度mm / 最低·中间值·最高重量g / 外径mm / 内径mm / 内孔要求 / 密度管控要求 / 实际密度管控下限·上限 / 跌落高度cm·次数·要求 / 测试间距mm·压头下降速度·强度要求kgf / 压降测试管路·流速·标准kpa / 外径公差 / 内径公差)。

**改动:仅 2 处口径对齐**
1. `内孔要求` 的 label 按需求正文改为 `炭棒内孔要求`(设计需求 0903:「炭棒内孔要求（备注文本栏）」)。
2. `密度管控要求` 的 placeholder 补齐为 `密度范围：~`(设计 `B26`)。

**不新增物理列** —— 现有实现里 `炭棒规格1/2/3` 三格 vs 设计一格 `炭棒规格`:保留三格(适应 外径×内径×长度 三值,已有单据这样用)。

---

### 2.7 🔧 RD_MOLD_FORMULA 成型配方(并入页 2)—— 配方表已一致,计算器待定

设计源:`最新烧结配方模板-1.xlsx` sheet `产品工单-配方` + `信息录入-配方计算器`。

**(a) 配方表(sheet `产品工单-配方`)** —— 标题 `炭棒配方管控清单` + 表单管理人/密级/使用范围/版本号 + 产品基本信息 + 配方表 + 配料要求。

| 设计列 | 现有 detail col | 结论 |
|---|---|---|
| No. | 序号 | ✅ |
| 物料种类 | 物料种类 | ✅ |
| 物料编号 | 物料编号 | ✅ |
| 物料名称 | 物料名称 | ✅ |
| 实际添加比例% | 实际添加比例 | ✅ |
| 单支物料含量g | 单支物料含量 | ✅ |
| 设计添加量 | 设计添加量 | ✅(设计注"百分比和数值混合",保持文本) |

**页 2 改动:无。**

**(b) 配方计算器(sheet `信息录入-配方计算器`)** —— 这是一张**会算的表**,不是纸面字段:

| 计算器区 | 字段 | 现状 |
|---|---|---|
| 基础信息 | 产品编号/产品名称/外径/内径/长度/生产车间/**一切几**/**折算比** | ❌ `一切几`/`折算比` 缺(`H2=0.65` 折算比) |
| 水分 | **物料平均水分** / **混合料水分参考** | ❌ 缺(`J2`/`K2`) |
| 尺寸与密度 | 模具尺寸/中心杆尺寸/管控密度下限/管控密度上限/产品管控类型/炭棒外观要求 | ⚠ 部分缺 |
| 设计密度 | **设计密度范围下限/上限** | ❌ 缺 |
| 明细三组 | 粉料/胶粉/**折算物料**(设计比例&添加量 / 水分含量 / 含水分材料数量) | ❌ 缺 |
| 密度管控 | 炭棒外径mm/内径mm(已有)、**密度管控等级**、密度范围允收规则 | ⚠ 部分缺 |
| 跌落强度 | 高度cm/次数/要求 | ✅ 已有 |
| 抗压强度 | 测试间距/压头下降速度/强度要求 | ✅ 已有 |
| 压降 | 管路/测试流速/压降标准 | ✅ 已有 |
| 成型长度 | **成型长度下限/标准/上限** + 各自体积 | ❌ 缺 |
| 重量 | **炭棒重量下限/标准/上限(干)** + **灌料下限/标准/上限(湿)** | 部分(灌料三值已作输入框) |
| 派生 | **实际水分** / 配方单 / 炭棒干重量标准未折算 / 折算炭粉量 | ❌ 缺 |
| 配方单明细 | 未折算物料前各种类物料重量 / 折算炭粉后添加量 / 添加比例 / 添加量 / 含水分添加量 | ❌ 缺 |

> ⚠ **是否把计算器做进面板是范围问题**(§5.2)。做 ⇒ 需新增上述派生列 + 前端计算逻辑;不做 ⇒ 页 2 保持现状,不新增任何列。
>
> 副作用:该 sheet 还有 `物料清单` sheet(84 种物料 + 水分含量)与 `烧结尺寸表` sheet(60 行 模具↔炭棒内外径公差),两者都是**基础数据/标准表**,不是面板字段 —— 建议落 `bs_inv`(物料水分)与新建 `rd_sinter_tolerance` 标准表,见 §5.2。

---

### 2.8 🔧 RD_ASM_PROC 组装工艺清单 —— 4 变体 + 变更履历

设计源两张:`关键控制清单--标准库.xlsx`(4 sheet 变体)+ `组装工艺控制.xlsx`(2 sheet)。

**(a) 关键控制清单 4 变体**

| 设计 sheet(变体) | 工序 | 当前实现 |
|---|---|---|
| `裸棒` | 无黑处理 / 机器除尘 / 尺寸外观检查 / 装箱 (**10 行**) | ❌ 无 |
| `机器包布` | 无黑处理 / 机器除尘 / 投首 / 机器包布 / 外观检查 / 装箱 (**16 行**) | ❌ 无 |
| `复合半成品` | 无黑处理 / 机器除尘 / 投首 / 放内布 / 包布套网 / 套折叠棉扎橡皮筋 / 套PP棉 / 外观检查 / 装箱 (**22 行**) | ❌ 无 |
| `成品` | 无黑处理 / 机器除尘 / 气枪吹灰 / 投首 / 机器包布 / 放内布 / 包布套网 / 套折叠棉扎橡皮筋 / 套PP棉 / 粘端盖 / 装垫片 / 外观检查 / …(两端及端盖LOGO / 侧面) / 装袋封口 / 塑封 / 贴标签 / 装箱 (**50 行**) | ⚠ **现有 seedRows 只有 21 条** |

> ⚠ **重大修正**:设计 sheet 用**合并单元格**(工序/检查比例纵向合并、工序控制内容横向多行),展开后「成品」是 **50 行**。现有 `RD_ASM_PROC_DT0.seedRows` 只有 **21 条** —— 差异不是"几条 `管控要求` 漏填",而是**首次复刻时把合并单元格塌缩了,丢了约 2.4 倍的行**。这不是设计变更,是既有实现缺陷。
>
> 实测 4 变体的 JSON 体积:裸棒 813 / 机器包布 1,347 / 复合半成品 1,654 / 成品 3,557 字符 —— 全部在 `StdLibController` 的 4,000 字符上限内,**一变体一条库条目可行**。

**(b) 标准库化**:设计 sheet `组装工艺控制-关键控制清单` 的 `H12 = 输入标准材料库` —— 即工序从标准库勾选填入,与现有 `dataTables.seedRows` 一次性硬编码预置不同。建议改为**标准库驱动**:4 变体 = `yj_std_lib` `asm.proc` 的 4 个 `item_code`,现有 21 条成品变体转为种子数据。(迁移方式见 §5.4。)

**(c) 组装BOM表**(设计 sheet `组装工艺控制-BOM表`)

```
标题: 惠州市银嘉环保科技有限公司 | 编号
     组装工艺控制-BOM表 | 表单管理人 / 密级 内部 / 使用范围 工艺科-组装车间 / 版本号
产品基本信息: 产品编号(录入 如 C-95-40) | 客户项目名称(自动填充规格书)
             产品功能类别(自动填充规格书) | 产品整体尺寸(自动填充规格书)
BOM表 (I12 = "BOM表引用规格书"):
  物料编号 | 物料名称 | 物料规格 | 外观要求 | 用量
```

| 设计列 | 现有 col_name | 结论 |
|---|---|---|
| 物料编号 | 物料编号 | ✅ |
| 物料名称 | 物料名 | ⚠ label 改为 `物料名称` |
| 物料规格 | 物料规格 | ✅ 已存在 |
| 外观要求 | 外观要求 | ✅ |
| 用量 | 用量 | ✅ |
| **客户项目名称**(自动填充规格书) | — | ❌ **新增** |
| **产品功能类别**(自动填充规格书) | — | ❌ **新增** |
| 产品整体尺寸(自动填充规格书) | `整体规格（外径）` + `整体规格（长度）` 两列 | ⚠ 保留两列,补"从规格书带回"refMap |

**(d) 变更履历**(设计 `组装工艺控制.xlsx` sheet `修订记录`)

设计列(`A2:E2` 表头,`D2:H2`/`I2:L2` 合并):`序号 | 日期 | 版本号 | 原因 | 内容`

| 设计列 | 现有 col_name | 结论 |
|---|---|---|
| 序号 | 序号 | ✅ |
| **日期** | — | ❌ **缺**(现有 `更改时间` 语义接近但键名不同) |
| **版本号** | — | ❌ **缺** |
| 原因 | 更改原因 | ⚠ label 改为 `原因` |
| 内容 | 更改内容 | ⚠ label 改为 `内容` |
| — | 责任人 / 备注 | 现有独有,建议保留 |

---

### 2.9 ✅ RD_INSP_PLAN 出货检验计划表 —— 按设计纸重排(2026-09-20 已实现)

设计源:`3.检验计划表\出货检验项目控制计划.xlsx`(表单编号 `YJ-QR-88`)。
迁移脚本 `tools/migrate-insp-plan-redesign-2026-09-20.sql`(15 批)+ i18n `tools/i18n-insp-plan-redesign.sql`(63 行),
配置在 `frontend/src/core/views/recordSheetConfigs.js` 的 `RD_INSP_PLAN`。

```
报告头: 惠州市银嘉环保科技有限公司 | YJ-QR-88
       出货检验项目控制计划 | 表单管理人 冯敏 / 密级 保密
产品编号(录入 如 C-95-40)      | 客户项目名称(自动填充规格书) | 使用范围(全公司)
产品功能类别(自动填充规格书)   | 产品整体尺寸(自动填充规格书) | 版本号(V+按照日期来)
编写人(编写人固定登录账号人员) | 审核人(固定：冯加劲)
明细表头: 序 号 | 检验项目 | 检验要求 | 检验方法 | 不合格应对措施 | 检查频率（下拉选择） | 备注
表尾: 若规格书有变动提示管控文件需更新
```

**四条口径(grill 定案)**:

| 议题 | 定案 |
|---|---|
| 明细表结构 | **一张表 7 列**,标准库 `insp.plan` 不动 |
| 检验方法键 | 用 **`控制方法`**(规范结构同源),不并 `检验内容` |
| 填充时机 | 选完产品编号即**全填**(表体已有行时先确认) |
| 表头取值源 | 用**规格书的等价列** |

**表头三行**(`sections`,照设计 r5/r6/r7 逐格对位;`产品编号`/`产品功能类别`/`编写人` 值格 `vspan:2` 跨 C:D,`版本号` 带 `rowspan:2` 续占 G:H):

| 设计字段 | 落地 | 结论 |
|---|---|---|
| 产品编号 | `产品编号`(参照 RD_PROD_INFO) | ✅ 复用 |
| **客户项目名称** | `客户项目名称`(新建,col 200) | ✅ 自动填充 ← 规格书 `客户项目名称` |
| **使用范围** | `使用范围`(新建,col 100) | ✅ 默认值「全公司」 |
| 产品功能类别 | `产品功能类别` | ⚠ **既有 参照**(见下「未决」) |
| **产品整体尺寸** | `产品整体尺寸`(新建,col 1000) | ✅ 自动填充 ← 规格书 `整体规格参数` |
| 版本号 | `版本号` | ✅ 默认值 `V@today` ⇒ `V2026-09-20` |
| 编写人 | `编写人` | ✅ `LOCKED_PERSON` = 登录用户(editable=0) |
| 审核人(固定 冯加劲) | `审核人` | ✅ 默认值 |
| 表单管理人 | `表单管理人`(col_name 仍是 `管理人`) | ✅ 默认值「冯敏」 |
| — | `主要性能` / `标题` / `客户名` / `滤芯尺寸` / `授权使用人` | 现有独有,保留但**已不上纸** |
| 公司名 | 全局引擎字符串 | ⚠ 设计原文无「科技」二字,引擎统一带「科技」,不改 |

**明细表**(一张表 7 列,`grid` 与可见列宽三处同宽 = 1187):

| 设计列 | 落地数据键(col_name) | 纸上显示(alias) |
|---|---|---|
| 序 号 | `序号` | `序 号`(alias 含空格,照设计) |
| 检验项目 | `控制项目` | `检验项目` |
| 检验要求 | `控制标准及要求` | `检验要求` |
| 检验方法 | `控制方法` | `检验方法` |
| 不合格应对措施 | `不合格应对措施` | 同左(默认 3 条文案) |
| 检查频率 | `检测频率` | `检查频率`(下拉:设计 3 选项) |
| 备注 | `备注` | 同左 |
| —(不上纸) | `检验类别` / `质量控制内容` / `检测仪器` / `检验` / `取样方式` / `检验内容` | `hiddenCol` |

⚠ **数据键一律写 label,纸上列名靠 `alias` 分流** —— `col_name` 是物理列名永不动,
`label` 才是前端行模型的键。本面板 5 处 `alias` 分叉(序号/控制项目/控制标准及要求/检测频率/控制方法)
即由此而来,`alias` 只落在本面板这一行,不影响别处同名字段。

**检验类别不上纸**(设计纸无此列):设计把明细分成「必测项/型式检验」两段,
但本面板只有**一张表**、连 `[表区]` 物理列都没有 ⇒ 不能走 `filterKey/filterVal`。
改由 `libGroupKey: '检验类别'` 承载,只在标准库勾选时记住行属哪个分组,该列同时 `hiddenCol`。
库里 13 行(必测项 7 + 型式检验 6)。

**「自动填充规格书」**(`autoFillSpec`)—— 设计 8 个格标了这四个字,语义是
「**按产品编号引用之后,把对应规格书的数据自动填进来**」:

| 落点 | 取值源(规格书 RD_SPEC_DOC) |
|---|---|
| 表头 `客户项目名称` | `客户项目名称` |
| 表头 `产品功能类别` | `产品类别` |
| 表头 `产品整体尺寸` | `整体规格参数` |
| 表体 `序号` | `序号` |
| 表体 `控制项目` | `检验项目` |
| 表体 `控制标准及要求` | `检验要求` |
| 表体 `控制方法` | `检验方法` |

实现:`onProdRefConfirm()` → `GET /api/px/specByProduct?code=<产品编号>`,
**不走通用查询链路** —— 规格书的 `编号`(=产品键)会被 `QueryService.loadDocs` 用单据编号覆盖,
通用查询取不到产品键。产品编号→规格书经 `rd_spec_assign` 分配表(优先)或 `rd_spec_doc_head.编号` 盖章;
多张时取最新并提示。表体已有行时先弹确认(不清库,追加);`defaults` 只补空格子,不覆盖已有内容。

**表尾提示**(`B10 = 若规格书有变动提示管控文件需更新`)—— 与既有 `SPEC_ITEMS_CHANGED` 通知机制同源,
**早已实现**,本次未动。

**未决(需业务口径,不在本次改动内)**:

- **`产品功能类别` 取值域冲突**:本面板该字段是**既有** `参照 → RD_PROD_INFO.产品功能类别`,
  而后者是下拉框 `除余氯 / VOC / 重金属`(性能功能词);`autoFillSpec` 却按设计把它填成规格书的
  `产品类别`(20 个品类词:普通炭棒/高精度炭棒/…),**两者取值域不相交**。
  照设计这一格只有「自动填充规格书」一个来源、没有手选路径,故合理落法是把该字段改回普通文本;
  但它属既有共享元数据,是否改口径待业务确认。当前行为:值能填、能打印,只是落在参照键域之外(参照弹窗选不中该项)。
- 设计此处为 `惠州市银嘉环保有限公司`(无「科技」),引擎全局统一为「惠州市银嘉环保科技有限公司」,以引擎为准。
- 库里 `控制方法` 的存量值与 `检测频率` 的下拉词表不符(库:常规抽检/型式检测报告;下拉:1PCS/一个生产批次、生产量*1%、型式检验),
  短检验方法文本实际存放在 `检测仪器`。属存量数据口径,未清洗。

---

### 2.10 🔧 RD_SPEC_DOC 规格书 —— 封面补 2 字段

设计源 `规格书细分.xlsx` 4 sheet,逐页对上:

| 设计 sheet | 现有页 | 结论 |
|---|---|---|
| `封面（产品信息）` | 页 1 产品信息 | ⚠ 见下 |
| `修订记录（履历表）` | 页 2 修订记录 | ✅(序号/更改内容/更改原因/更改时间/责任人/备注) |
| `检验项目及标准` | 页 3 标准库勾选 | ✅(18 组,库内 26 组) |
| `成品及包装运输` | 页 4 | ✅ |

**封面差异**(设计 `B7:C15`):

| 设计格 | 现有 cover field | 结论 |
|---|---|---|
| 编 号 | `编号` | ✅ |
| 产品类别 | — | ⚠ 设计此处值是 `伊可普高品质功能炭棒`(≠ RD_PROD_INFO 的 20 项产品类别),实为**产品名称/品类描述** |
| 客户名称 | `客户名` | ⚠ label 改为 `客户名称` |
| 客户料号 | `客户料号` | ✅ |
| **客户项目名称** | — | ❌ **新增** |
| **应用场景** | — | ❌ **新增**(设计 `C12` = 抽水壶) |
| 整体规格参数 | — | ⚠ 现有为 `整体规格参数` 章节字段,确认口径 |
| 产品主要性能 | — | ⚠ 现有为 `产品主要性能` 章节字段,确认口径 |
| 版 本 | `版本` | ✅ |
| 制订/审核/批准/日期 | `制订日期`/`审核日期`/`批准日期` | ✅ |

---

### 2.11 🟢 数据记录表·实验室 4 面 —— 无改动

| 设计 | 面板 | 结论 |
|---|---|---|
| `加标水配置记录表.xlsx` 3 sheet(除铅 PH8.5 / 除汞 PH8.5 / 除VOC) | RD_SPIKE_WATER | ✅ 3 变体动态列已实现(21 detail 列 = 并集) |
| `测试申请单.xlsx` `内部1` + `外部` | RD_DOM_TEST | ✅ 2 变体已实现(内部 20 列全部对上) |
| `设备使用登记表.xlsx` 7 sheet(加标测试系统 1#~7#) | RD_EQUIP_USE | ✅ 表头 = `设备名称`,7 个 sheet 即 7 条设备值 |
| `仪器使用记录表.xls` `使用1`/`使用2` | RD_INSTR_USE | ✅ 9 detail 列对上 |

> `测试申请单` 设计 sheet `汇总表`(序号/表格编号/发起人/申请日期/分类/状态)属面板列表视图既有能力,不新增字段。

---

### 2.12 🟢 RD_SHARE_FILE 共享文件库 —— 无改动

设计 `共享文件项目.xlsx`:3 大类 6 子类 ——
`标准`(企标 / 国标·行标 / NSF标准)、`测试报告`(出口所需报告 / 有害物质 ROSH·REACH)、`产品认证`(涉水批件 / 认证)。

现有面板是 `文件`(附件)单列的专用视图,分类为**视图内导航**,非字段。**改动:无**(把 3 类做成 `类别` 字段属增强,见 §5.9)。

---

## 3. 新增面板设计

### 3.1 ➕ RD_PROD_DOCLIST 产品文件列表

**设计依据**:`产品开发系统需求汇总.xlsx` → sheet `文件汇总表`

```
产品文件列表
可通过产品编号直接搜索
产品编号 | 文件1 | 状态 | 文件2 | 状态 | 文件3 | 状态 | 文件4 | 状态 | 产品负责人 | 是否受控 | 受控日期
C-95-33  | 规格书 | 编写/已审核 | 成型工艺清单 | 编写/已审核 | 组装工艺清单 | 编写/已审核 | 出货控制计划 | 编写/已审核 | xxx | 是 | 2026-09-18
```

配套流程(同 sheet `B29:D38`):

| 序号 | 表单 | 部门 |
|---|---|---|
| 1 | 产品信息表 | 销售 |
| 2 | 一级审核 | 冯总 |
| 3 | 二级审核 | 秀丽 |
| 4 | 任务分发 | 秀丽 |
| 5.1 | 规格书 | 项目负责人 → 保存/提交 → 提交后审批 → 审批后自动受控 |
| 5.2 | 成型控制要点 | 刘磊 |
| 5.3 | 组装控制要求 | 柴善银 |
| 5.4 | 出货控制计划 | 冯敏 |

**★ 关键发现:这张表已经存在,只是以按钮形式藏在产品信息表里。**

`DevTaskService` 的 `DEV_PANELS` 矩阵恰好就是设计的 4 个文件列,顺序也一致:

```
RD_MOLD_PROC 成型工艺清单 / RD_ASM_PROC 组装工艺清单 / RD_SPEC_DOC 规格书 / RD_INSP_PLAN 出货检验计划表
```

状态推导口径也一致(存 `rd_dev_task` 下发记录 + 下游单据 + `yj_doc_status` 实时推导,不另存状态):

```
已归档 → 开发完毕 | 审批中 → 开发审核中 | 有草稿 → 开发中 | 无单据 → 未开发
```

**因此本面板 = 把 DevTaskService 的矩阵暴露为可查询列表面板**,不新建业务表、不双写数据。

| 列 | 数据键 | 类型 | 来源 |
|---|---|---|---|
| 产品编号 | `产品编号` | 参照 RD_PROD_INFO | RD_PROD_INFO 全量 |
| 文件1~4 | 固定 4 列(面板列,非动态) | 显示 | `DEV_PANELS` 顺序 |
| 状态1~4 | 派生只读 | 显示 | 见上推导 |
| 产品负责人 | `产品负责人` | 文本 | RD_PROD_INFO(参照带回) |
| 是否受控 | `是否受控` | 下拉 是/否 | 4 文件全归档 ⇒ 自动 `是` |
| 受控日期 | `受控日期` | 日期 | 第 4 个文件归档日 |

**新增存储**:`rd_prod_doclist_head` + `rd_prod_doclist_detail`(行:产品编号 / 是否受控 / 受控日期,共 3 列)。

**实现方式**:`mode='doc'` 单单据面板(同 RD_PROGRESS 模式)+ 矩阵渲染分支(复用 `ProgressControlSheet.vue` 形态);新增只读端点 `GET /px/prodDocList`(内部复用 `DevTaskService` 状态推导)。

**新增多语言**:面板名 + 3 个字段 × 11 语言(按 AGENTS.md 硬性规定)。

---

### 3.2 ➕ RD_SAMPLE_NO 样品编号表

**设计依据**:`二三级四级项目控制表2026.xlsx` → sheet `产品开发样品编号`

```
标题: 产品开发样品编号 | 密级 绝密 | 适用范围 工程技术中心
样品编号 | 项目名称 | 子项目 | 项目负责人 | 项目编号 | 炭棒尺寸
FL201-1  | 飞利浦抽水壶芯 | 低配款 | 陈秀丽 | 201-1 | 48*30*130
…
表尾编号规则: XX-XXX-01
  XX  = 客户项目代号
  XXX = 项目编号
  01  = 编号序号
```

| 字段 | 数据键 | 类型 | 备注 |
|---|---|---|---|
| 样品编号 | `样品编号` | 文本(必填,唯一) | 按规则生成,可手改 |
| 项目名称 | `项目名称` | 文本 | 参照 RD_PROGRESS.项目名称 |
| 子项目 | `子项目` | 文本 | |
| 项目负责人 | `项目负责人` | 文本 | |
| 项目编号 | `项目编号` | 文本 | |
| 炭棒尺寸 | `炭棒尺寸` | 文本(多行) | 设计有换行值 |

**新增存储**:`rd_sample_no_head` + `rd_sample_no_detail`。

⚠ **编号规则不自洽,实现前需确认**(§5.5):设计样例 `FL201-1` 只有 2 段,而规则写 3 段 `XX-XXX-01`;且 `F5 = 201-1` 项目编号本身已含 `-`。

---

## 4. 改动汇总(实现清单)

| 面板 | 新增物理列 | 新增字段 | label 对齐 | 口径修正 |
|---|---|---|---|---|
| RD_PROGRESS | 13 列 | 13 | 3 | 修 `progressColumns.js` 的 `pendingAlign` 与错位映射 |
| RD_PROD_INFO | 3(客户项目名称 / 产品管控等级 / 产品功能类别) | 3 | 4 | 三分类字段收口 + 两级审核人 |
| RD_ASM_PROC | 4(客户项目名称 / 产品功能类别 / 日期 / 版本号) | 4 | 5 | 4 变体标准库化;`管控要求` 补全文 |
| RD_INSP_PLAN | 3 表头(客户项目名称 / 产品整体尺寸 / 使用范围)+ 2 明细(序号 / 备注) | 5 | 5(alias 分流) | 见 §2.9:一张表 7 列 + 自动填充规格书;检测频率改下拉;版本号默认 `V+日期` |
| RD_SPEC_DOC | 2(客户项目名称 / 应用场景) | 2 | 1 | — |
| RD_MOLD_PROC | 0 | 0 | 2 | 提示语补齐 |
| RD_MOLD_FORMULA 页 | 待定(§5.2) | 待定 | 0 | — |
| RD_PROD_DOCLIST | 新表 3 列 | — | — | 暴露 DevTaskService 矩阵 |
| RD_SAMPLE_NO | 新表 6 列 | — | — | 编号规则待定 |

**全会话唯一的新概念字段(跨 4 个面板复用)**:
`客户项目名称` · `产品功能类别` · `产品整体尺寸`

三者设计上都标 **"自动填充规格书"**,即从 RD_SPEC_DOC 带回。建议**一次性在 `refMap` 里建这三条映射**,避免 4 个面板各写一遍(该 refMap 容器仍是 `docs/development/技术债清单.md` 第 1 条未落地项 —— 本次正好一并落地)。

---

## 5. 未决问题(需你定见)

1. **`产品信息表内容.xlsx` 的 `产品类别` 20 项**是否替换现有字典?建议:替换,并把现有 7 项历史值搬到新增的 `产品功能类别`(见 §2.5 真实映射表)。
2. **配方计算器**是否落地到面板?它是**会算的表**(按配方自动算灌料三值、含水量、折算炭粉量),落地意味着新增派生列 + 前端计算逻辑;不落地则 `一切几/折算比/实际水分/折算炭粉量/成型长度三值` 无需建列。附带:`物料清单` sheet(84 种物料 + 水分含量)与 `烧结尺寸表` sheet(60 行 模具↔炭棒内外径公差)是标准数据,建议落 `bs_inv` 与新建标准表。
3. ~~`管控要求` 空值补全~~ → **已量清,问题转化**:设计展开后成品是 50 行、现有 seedRows 只有 21 条,不是"补几条空值"而是**按设计重建 4 变体**(§2.8)。仅需确认口径。
4. **4 变体标准库化 vs 保留硬编码 seedRows**:设计标注"输入标准材料库",实测 4 变体 JSON 体积(813/1,347/1,654/3,557)全部在库条目 4,000 字符上限内,**方向可行**,建议走 `yj_std_lib`(`asm.proc`)。
5. **样品编号规则**:设计样例 `FL201-1` 与规则 `XX-XXX-01` 不自洽,以哪个为准?
6. **`产品文件列表` 的"状态"是派生还是存表**:建议派生(与 RD_PROGRESS 同构、不双写);若业务要手工覆盖,则需存表。
7. ~~**`客户项目名称` 等三个新字段的归属**:是 RD_PROD_INFO 独有 + 其余面板参照带回,还是各面板独立存列?建议前者。~~
   → **2026-09-20 出货检验计划表选了「各面板独立存列」**:设计那几格标的是「自动填充规格书」,
   取值源是**规格书**(`rd_spec_doc_head.客户项目名称 / 整体规格参数`),不是 RD_PROD_INFO;
   做成参照反而取不到值。故 `客户项目名称 / 产品整体尺寸 / 使用范围` 在 `rd_insp_plan_head` 各存一列。
   ⚠ 与 §2.5 的 RD_PROD_INFO 同名字段是**两套数据**,不是同一列 —— 若将来要收口成一处,需先定哪边是权威。
8. **`系统样品模板.xlsx`**(销售样品单 / 开发样品申请单)不在本次范围 —— 它对应制造模块 `SAMPLE_REQ` 样品申请单与销售样品流程,建在研发管理会重复。确认跳过?
9. **`共享文件库` 是否加 `类别` 字段**(标准 / 测试报告 / 产品认证),还是保持现状的视图内分类?
10. **新增参照是否要"只列已归档"过滤**?现有参照过滤(`filter={单据状态:已归档}`)只对 `RD_APPROVAL` 硬编码生效;本次新增的 `客户项目名称`/`产品功能类别` 指向 `RD_PROD_INFO`、`项目名称` 指向 `RD_PROGRESS`,**没有过滤** ⇒ 会列出草稿单。

---

## 6. 已敲定决策

| 日期 | 决策 | 来源 |
|---|---|---|
| 2026-09-18 | 对照范围:**全 22 面都出条目**,改动大的做完整设计 | grill 第 1 问 |
| 2026-09-18 | 新增面板范围:**只做「产品文件列表」+「样品编号表」**;销售样品单/开发样品申请单不做(属制造/销售域) | grill 第 2 问 |
| 2026-09-18 | RD_PROGRESS:**补齐物理列,18 列全部真落库**,消掉 `pendingAlign` 与显示名↔列名错位 | grill 第 3 问 |

---

## 7. 决策树小结

```
面板清单(22)              → 已定(3 组 22 面,yj_panel 实测)
├─ 已一致(12)            → 已定(不动,只出"无改动"结论)
├─ 需改(7)               → 已定(差异已逐字段列出)
│   ├─ RD_PROGRESS       → 已定(补物理列,18 列真落库)
│   ├─ RD_PROD_INFO      → 已定(3 新字段 + 4 label + 两级审核人 + 三分类收口)
│   ├─ RD_ASM_PROC       → 已定(2 引用字段 + 变更履历 2 列 + 按设计重建 4 变体)
│   ├─ RD_INSP_PLAN      → 已定(表头 2 + 明细 2 + 检测频率改下拉 + 编写人锁定)
│   ├─ RD_SPEC_DOC       → 已定(封面 2 字段)
│   ├─ RD_MOLD_PROC      → 已定(label/提示语,不新增列)
│   └─ RD_MOLD_FORMULA   → 部分(配方表一致;计算器是否落地 → 未决 2)
└─ 新增(2)               → 已定(产品文件列表 / 样品编号表)
    ├─ 产品文件列表       → 已定(暴露 DevTaskService 矩阵,派生状态)
    └─ 样品编号表         → 未决(编号规则不自洽 → 问题 5)

字段能力层(9 维)         → 已定(契约真源 + 51 字段逐字段矩阵,见实施设计 §1/§2)
├─ 数据类型分派           → 已定(下拉框/标准库/参照 三分派;标准库 dict_sql 存库编码)
├─ 可编辑性               → 已定(仅派生列 editable=0;编写人按人字段锁定机制)
├─ 必填                   → 已定(新列一律 required=0,仅样品编号=1 → 避幽灵必填)
├─ 唯一性                 → 已定(文档编号走既有 checkDocNoUnique;样品编号需新增校验)
├─ 参照与过滤             → 部分(指向清晰;是否加"只列已归档"过滤 → 未决 10)
├─ 显隐两套语义           → 已定(hidden/visible 三组合的判别规则)
├─ 归档闭环               → 已定(2 新面板入 DOC_ARCHIVE_PANELS)
└─ 多语言                 → 已定(改 label 必须同步译名,清单已出)
```

---

## 附:本会话产出

| 文件 | 内容 |
|---|---|
| `_rd-work\design\*.txt` | 25 份设计文件的全量文本提取 |
| `_rd-work\rd_fields.tsv` | 729 行 `yj_field` 实测导出 |
| `_rd-work\panel_fields.md` | 22 面板字段清单(header/detail/query) |
| `docs\design\研发管理-面板与设计对照.md` | 本文件 |
| `docs\design\研发管理-新面板设计与改动方案.md` | 实施设计(迁移脚本 + 分阶段) |
