fix: 送料暂收单收敛期新增字段块隐藏归位 + 链路字段核验(两账套)

用户报:暂收单原本隐藏的字段(条码~折扣)被翻开且排最前;链路表头未对齐;转ERP记录要保留;批次号问题。
查证与处置:
- 根因:第一轮合并收敛时 migrate-qc-recv-fields 首次执行(本地从未跑过),把 条码/发货数量/
  剩余数量/退料数量/报废数量/制单号/折扣金额/品质复核人/品质复核时间 以可见态灌入 QC_RECV
  ——13:23 快照(最早备份)已带此污染,故 yj_field 整表快照还原无法去除;
- 处置:该 9 字段块 hidden=1/visible=0/seq 挪 900+,两账套同修;明细可见列表恢复干净
  (单号/物料编码/行号/物料名称/规格/批次号/…/税率%),查询弹窗已不可选;
- 转ERP记录核验完好:PURCHASE_IN/SALE_OUT/PU_ORDER 的 是否已转ERP/ERP单号/操作人/时间
  四行 hidden=1 在位,数据列与值(ERP单号 CGRK-…)都在;
- 批次号结构核验:五面板 头行分层正确(表头可见/明细可见/批次键隐藏),如仍有具体异常待用户指认;
- 空库链重建尝试(HSDZ_MES_CHAIN)因古基座脚本依赖已删列中止,临时库已清;
  表头调整直接写 yj_field 的机制已查明——残余表头排布可由用户界面「表头调整/表格调整」自校。