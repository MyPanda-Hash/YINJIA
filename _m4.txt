fix: 供应链库侧污染深度修复——yj_field 整表快照还原 + 仓库列误删复原(两账套)

用户报(代码回滚后仍有):隐藏位被翻开、供应商编码没了、特采按钮没了、暂收单变草稿。
逐项查证(对照 13:23 快照):
- yj_field 被 panda 系列重放整体重灌:PU_ORDER 丢 7 行(含供应商编码)、PURCHASE_IN 的
  ERP单号/转ERP操作人/转ERP时间/采购订单号 hidden 1→0、INV.来料检验列翻开;
- 特采(QC_INSP)字段行其实在;暂收单在快照里状态行也是 0(该面板不走状态机,一直显示草稿,
  非本次污染;审核功能正常,当前库有 admin 审核痕);
- 连环损伤:pin-wh-unify 重放把正名 [仓库] 列当旧列 DROP(bl_purchase_in 仓库数据列+字段行全丢)。
修复(两账套):
- yj_field 整表从快照还原(=回滚后代码的配套字段态),再重放 pin-wh-unify/wh-field-rename/
  po-push-erp 三个后快照合法字段迁移;
- bl_purchase_in.[仓库] 列重建,数据从快照两列合并复原(旧仓库209+旧仓库名称12-重叠=216 行);
  PURCHASE_IN.仓库 明细行(参照WH/必填/可见)重建;SO_ORDER 隐藏行 required 降 0 防拦保存;
- pin-wh-unify 补三处重放守卫(回填动态SQL/删列双列并存才删/自检终态口径+RAISERROR 参数
  只收变量);取回 6 个与库内登记哈希一致的脚本版本(doc_part1/panda×2/manu-prune/wo-report/
  status-align,否则按回滚后旧字节重放又炸)。
验证:两账套 DbSync 失败 0;rename 冒烟 0 FAIL(采购入库 74 张/销售出库 54/台账 235/状况表 84);
质检链+暂收单健康;仓库列 216 行存活、字段行就位;临时还原库已删。