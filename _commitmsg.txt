feat: 转ERP单位解析补韧性——目标账套缺单位时强制刷新22h缓存重试一次

踩坑经过(2026-09-23 沙箱实测):用户界面推 PI-2026-09-0128 报
「第1行计量单位[张]在当前账套金蝶单位档案中无对应ID」:
  1. 该单两行 计量单位=张 且行上 单位id 为空(检验单自动生成,链路不带单位id);
  2. 沙箱账套单位档案只有 11 个(个/件/包/支/瓶/箱/袋/g/kg/元/次),无「张」;
  3. 解析不到 unit_id 按设计硬拒(宁拒不推错单位)——报错本身正确。
处置:API 在沙箱补建「张」(deploy/_add-sandbox-unit-zhang.mjs,id=2574929687809865728);
但后端单位缓存 22h(unitIdByName),失败尝试已灌入旧表,补建后重试仍会命中旧缓存,
故 KingdeePushService 在按名称解析不到时强制失效缓存重拉一次再试,免去补单位后必须重启后端。
另:沙箱单位盘点探针 _probe-sandbox-units.mjs;已推订单 CGDD-20260909-00001 行上带的
单位id=2167(同步来源账套的ID)不在沙箱档案,金蝶静默收单——界面验证时留意单位列显示。