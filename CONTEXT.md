# CONTEXT — YINJIA-MES 领域术语表

> 术语表(glossary),非实现规范。由 grill-with-docs 拷问会话维护。
> 姊妹项目 light-mes 的术语表见 `../light-mes/CONTEXT.md`(面板引擎同源)。

## 术语

### 多国语言切换(Locale Switching)

像输入法一样的即时语言切换:顶栏下拉 + `Alt+L` 快捷键循环切换,
vue-i18n 响应式热生效(不刷新页面),按用户记忆(`yj_user.locale`),
未登录时回退浏览器 `localStorage`。

### 翻译分层(Translation Layers)

| 层 | 内容 | 存放 |
|---|---|---|
| UI 文案层 | 菜单、按钮、提示、布局文本 | 前端 vue-i18n 语言包(`zh-CN.ts` / `en.ts`) |
| 面板标签层 | `yj_field.label` 字段标签 | SQL Server 译名列,随面板配置接口下发 |
| 字典数据层 | 仓库/工序/类别等 `dm_*` 字典名称 | SQL Server 译名列 + 维护界面 |

### 字典翻、事实不翻(Dictionary-only Data Translation)

翻译边界约定:**字典/配置类数据**(仓库"原料仓"、工序"检验人员")提供
译名并随语言切换;**业务事实类数据**(客户法定名称、单据编号、用户录入
备注、物料规格)保持原文,不做机器翻译——公司名与规格型号的机器译文
不可控,保持原文是准确性要求,不是缺失。

### 使用权限查看(Usage Review)

顶栏用户下拉内的管理员入口(与「组织架构」同级,仅 `is_admin='Y'` 可见):
查看**所有账号**的登录记录与面板权限使用记录。数据存 `yj_usage_log`(事件类型
`login`/`action` 区分登录与操作),由登录接口与面板按钮入口统一埋点写入;
查询/刷新/表格调整等纯浏览动作不记录。旧系统遗留表 `s_log` 的历史数据不并入展示。

## 已敲定决策

| 日期 | 决策 | 来源 |
|---|---|---|
| 2026-08-30 | 语言范围:简体中文 + 英语(架构可扩展,后续加语言只加语言包) | grill 会话第 1 问 |
| 2026-08-30 | 数据翻译边界:字典翻、事实不翻(不接机器翻译) | grill 会话第 2 问 |
| 2026-08-30 | 翻译存储:前端语言包(UI)+ 数据库译名列(面板/字典)分层 | grill 会话第 3 问 |
| 2026-08-30 | 切换交互:顶栏下拉 + Alt+L 快捷键,按用户记忆(yj_user.locale),热切换不刷新 | grill 会话第 4 问 |
| 2026-08-30 | 语言扩展路线:**通用翻译表(任意语言=加行不加列)+ 动态语言列表 + 常用语言包人工维护 + 未覆盖词条阿里云翻译 API 实时兜底(结果缓存回翻译表)+ 管理员可校对**。UI 文案机翻可接受(ADR-0001 的"事实不翻"仅约束业务数据) | grill 会话 2026-08-30 晚 |
| 2026-09-02 | 使用记录存储:新建 `yj_usage_log`(登录+操作统一一张表),不复用旧系统 `s_log`、不展示其历史 | grill 会话「使用权限查看」第 1 问 |
| 2026-09-02 | 使用权限查看入口仅管理员可见(登录用户不可见);记录范围为业务动作+登录成功,不含查询/刷新类浏览动作 | grill 会话「使用权限查看」第 2、3 问 |

### 已审批判据(Approved Status Criteria)

单据"已审批"的统一判据(对应 light-mes `core/doc-status.js` 语义;YINJIA 由
`yj_doc_status` 状态推导实现同一判定):状态 ∈ {已审核, 生产中, 已完工, 已关闭}
或 审批状态 ∈ {已审批, 已通过}。列表/表单共用同一判据,禁止各处手写状态集合。

### 翻译表(Translation Table)

`yj_translation(scope, ref_key, locale, text)`:任意对象(面板/字段/前端词条)的任意语言译名存行,
替代按语言加列(label_en/label_ja...)的不可扩展模式。`yj_locale` 为语言注册表(可启用语言列表)。
机翻兜底:翻译表 miss 时调阿里云翻译(与 OCR 同厂同凭证),结果写回翻译表成为缓存。

## 遗留问题(记录在案,未决)

- Element Plus 组件内置文案(日期选择器、分页等)需配 `zhCn`/`en` locale 同步切换
- 后端返回的消息(错误/成功提示)采用"消息码+参数"模式由前端翻译;存量硬编码中文消息需逐步迁移
- 日期/数字格式(本地化)随 locale 切换的粒度待实施时定
