# CONTEXT — YINJIA-MES 领域术语表

> 术语表(glossary),非实现规范。由 grill-with-docs 拷问会话维护。
> 姊妹项目 light-mes 的术语表见 `../light-mes/CONTEXT.md`(面板引擎同源)。

## 术语

### 多国语言切换(Locale Switching)

像输入法一样的即时语言切换:顶栏下拉 + `Alt+L` 快捷键循环切换,
vue-i18n 响应式热生效(不刷新页面),按用户记忆(`yj_user.locale`),
未登录时回退浏览器 `localStorage`。

### 翻译分层(Translation Layers)

| 层 | 内容 | 存放 |
|---|---|---|
| UI 文案层 | 菜单、按钮、提示、布局文本 | 前端 vue-i18n 语言包(`zh-CN.ts` / `en.ts`) |
| 面板标签层 | `yj_field.label` 字段标签 | SQL Server 译名列,随面板配置接口下发 |
| 字典数据层 | 仓库/工序/类别等 `dm_*` 字典名称 | SQL Server 译名列 + 维护界面 |

### 字典翻、事实不翻(Dictionary-only Data Translation)

翻译边界约定:**字典/配置类数据**(仓库"原料仓"、工序"检验人员")提供
译名并随语言切换;**业务事实类数据**(客户法定名称、单据编号、用户录入
备注、物料规格)保持原文,不做机器翻译——公司名与规格型号的机器译文
不可控,保持原文是准确性要求,不是缺失。

### 使用权限查看(Usage Review)

顶栏用户下拉内的管理员入口(与「组织架构」同级,仅 `is_admin='Y'` 可见):
查看**所有账号**的登录记录与面板权限使用记录。数据存 `yj_usage_log`(事件类型
`login`/`action` 区分登录与操作),由登录接口与面板按钮入口统一埋点写入;
查询/刷新/表格调整等纯浏览动作不记录。旧系统遗留表 `s_log` 的历史数据不并入展示。

### 数据记录表面板(Record Sheet Panels)

研发管理「数据记录表」一族文书面板(RD_FILTER_EFF 功能性滤效 + 其余 7 张:碱性/矿化/抑菌/
阻垢性能/RO保护/浸泡安全/压降精度),按《04数据记录表.xlsx》一比一复刻纸面版式。
统一形态:`<表>_head`/`<表>_detail` 双表中文列,保存即归档(退出草稿状态机)。
**文档编号 = 参照立项申请(RD_APPROVAL)的「文档编号」**(2026-09-09 起,8 面板统一):
即立项申请表**纸张右上角**那一格的值(YJ-XSxxx 口径),两张纸右上角编号一致;
立项申请归档后数据记录表按项目落记录——同一面板内文档编号不重复(一个项目一张记录表),
跨面板可复用同一立项申请;点右上角编号弹参照选择,密级随参照带回。
**两侧均必填**(required=1,且列上模板号 DEFAULT 已删除,新单该格为空):立项申请不填编号,
引用过来就是空值;数据记录表不填编号,项目归属断链。参照弹窗**只列已归档立项申请**
(草稿/审批中项目编号未定稿,引用会落空)。
**这一格的提示词与标签一律称「文档编号」,不称「单据编号」**:同面板另有「单据编号」,
是系统自动生成、经办人不可编辑的流水号(如 LXA-2026-09-0005/LXB-2026-09-0008),
与纸张右上角那一格不是同一件事;提示词指向它会把人引去填一个填不动的字段。
文档编号保存时校验唯一;功能性滤效为独立组件,其余 7 张由 `RecordSheetPanels.vue` +
`recordSheetConfigs.js` 配置驱动渲染(sections 条件区/两级表头数据表/结论区/特例块)。

- 边界:Excel 原表无结论区的表(碱性/矿化/阻垢/压降)不设结论字段,忠实原版。
- **参照带回不覆盖本单标识字段**:面板分组列/单据号列/日期列/主键列(单据编号/单据日期等)永不参与
  refMap 自动映射——单据→单据参照(数据记录表→立项申请)同名标签会冲掉本单单号,唯一性校验误判重复;
  业务字段(密级等)照常随参照带回。
- 矿化 4 个指标块共用一张明细表,以「指标」字段(锶/偏硅酸/PH/TDS)分块渲染并配散点图。
- 浸泡安全进入草稿编辑时自动预填 GB/T17219 标准 17 项卫生项目(Excel 原表即预填)。

实验室使用记录表 4 面板(加标水配置/内部委托测试申请/设备使用登记/仪器使用记录,
按《3.实验室使用记录表》复刻)同用该渲染器,两种版式:
- report 版式(委托单):公司名+大标题+右侧信息块(文件管理人/密级/文件使用范围),文档编号 YJ-RIR001 参与唯一校验;
- plain 版式(其余 3 张):标题条+副标题行(测试项目：/设备名称：/仪器名称/型号：),无公司报告头。
- 动态列变体(variantKey):加标水按测试项目切换除铅/除汞/除VOC 列集(明细为全字段并集,隐藏列数据保留);
  委托单按申请单类型切换开发性/品质委托(标题随类型派生,加行自动补序号)。

### 产品文件文书面板(Product Document Panels)

研发管理「产品文件」下 7 个面板,按产品开发《2.产品文件》源文档一比一复刻
(源文档位于 `C:\Users\x1787\OneDrive\Desktop\产品开发\2.产品文件`),与数据记录表共用
RecordSheetPanels 渲染器(配置驱动,key=yj_field label 中文数据键):

- **产品信息表**(RD_PROD_INFO):14 字段(产品编号/名称/类别/类型/整体尺寸/客户料号/炭棒尺寸/
  特殊性能描述/下单数量/分类/形态/客户图纸或规格书/责任人/审核人),2026-09 起并入文书面板
  (RecordSheetPanels 纸张式):报告头(公司名+单据编号+单据日期信息块)+ 四节键值对网格
  (一、产品基本信息/二、规格与尺寸/三、特殊性能描述/四、文件与签署),下拉走 yj_field 字典,
  长文本(特殊性能/备注)为整行多行域;此前为普通 doc 面板。**「客户图纸或规格书」为附件字段**
  (2026-09-11 起,recordSheetConfigs type='file' → FileAttachCell):多附件、保留原文件名、
  点击查看、打印/导出 PDF 只显示文件名(见「附件字段」术语)。
- **成型工艺清单**(RD_MOLD_PROC):炭棒工艺管控清单——报告头(表单管理人/密级/使用范围/版本号)+
  产品基本信息 + 工序(灌料/烧结/热压/冷却/脱模)+ 检验要求;工序与检验要求的键值对块统一
  「标签行 + 值行」两行式(源 Excel 网格:标签占外列,值占内列)。密度管控要求输入框提示语
  「密度范围:~」。
  **2026-09-11 起与「成型配方」并为一张单两个页签**:页 1 = 成型工艺清单,页 2 = 成型配方
  (原 RD_MOLD_FORMULA 的产品基本信息 + 配方表 + 配料要求)。**两页各用各的原始版式**(页 1 = 11 列
  网格 [130,110,70,100,70,70,70,100,70,125,125],页 2 = 13 列
  [101,60,109,85,52,52,52,146,64,64,121,77,57];报告头跨度与标题也各按各的)——见「每页版式」术语。
  页 2 的配方行落在**同一张行表** rd_mold_proc_detail,用「表区=配方表」分块(filterKey 模式);
  配料要求是头字段(rd_mold_proc_head.配料要求)。
- **成型配方**(RD_MOLD_FORMULA):炭棒配方管控清单——产品基本信息(标签行/值行两行式)+
  配方表(No./物料种类2/物料编号4/物料名称3/实际添加比例/单支物料含量/设计添加量,13 格,
  合计行)+ 配料要求。**2026-09-11 起本面板已并入 RD_MOLD_PROC 第 2 页签并从菜单下线**,
  yj_panel/yj_field/yj_role_panel 行保留(便于回滚);recordSheetConfigs 里该配置**仍在生效**
  (提到模块级常量,作为页 2 的唯一真源被主面板按页引用);已有单据已清理。
- **组装BOM表**(RD_ASM_BOM):产品基本信息(编号/名称/种类/规格外径长度/成品重量)+ 修订记录
  (序号/更改内容/更改原因/更改时间/责任人/备注)+ 炭棒滤芯组装/包装物料清单(29 种物料预置,
  含规格模板与外观要求);源文档无信息块但**有自己的 report 报告头**(标题=组装BOM表)。**2026-09-11 起
  本面板已并入 RD_ASM_PROC 第 2 页签并从菜单下线**,行与配置同样保留、已有单据已清理。
- **组装工艺清单**(RD_ASM_PROC):炭棒滤芯组装/包装段-关键工序控制清单,plain 清单式,
  21 道工序预置(控制内容/管控要求/检查比例全文预填)。
  **2026-09-11 起与「组装BOM表」并为一张单两个页签**:页 1 = 关键工序控制清单(plain 原样,
  本表自持列宽 130/320/430/120、表宽 1000,顶部补「产品基本信息:产品编号/产品名称」,
  产品编号参照产品信息表——产品开发下发矩阵的关联键),页 2 = 组装BOM表(**report 原样**:
  报告头 + 4 列网格 [130,390,130,390] + 产品基本信息 6 格 + 物料清单 + 修订记录);
  两张 BOM 逻辑表落在同一张行表 rd_asm_proc_detail,用「表区=物料清单/修订记录」分块。
- **每页版式**(per-page layout,2026-09-11 返工新增):一张单的两个页签**各按各自原设计渲染**,
  互不迁就。`cfg.pages[i]` 可声明 `grid`(本页列网格)/ `headMode`('report'|'plain')/
  `head`(本页报告头跨度)/ `showHead`(本页是否画报告头)/ `staticTitle`(本页大标题);
  未声明的项退回面板级 `cfg.*`。生效顺序:`pages[i].x` → 变体 `variants[].x` → `cfg.x`
  (网格另有区块级 `sec.cols` 优先)。**为什么需要它**:引擎原先 `effGrid` 只认
  `cfg.grid`(区块没有 `sec.grid` 回退),两张版式不同的单据并成一张单后只能共用一个网格,
  一页必然被另一页的列宽挤变形——2026-09-11 首次合并就是这么把两页设计改坏的,故按页拆开。
- **页签归页与共表口径(2026-09-11)**:`cfg.pages` 数组长度=页签数,区块用 `page` 索引归页
  (sections / tailSections / tailDocSections / dataTables 四类都支持,**tailSections 原先不按页过滤,
  已补齐**);一次只打印当前页签。
  **「表区」是物理列而非内存标记**:多张逻辑表共用一张行表时,表区必须既在 yj_field 登记
  (place='header',否则保存链 labelsToCols 把未声明的键丢掉、行下次打开就消失),又在头表有同名物理列
  (否则 QueryService.selectCols 生成 t.[表区] 报"列名无效",整面板查询 500)。
- **规格书**(RD_SPEC_DOC):**单面板、9 种规格书类型仅作单据分类**(页签 Tab 过滤,新单默认当前类型),
  4 页结构(产品信息封面/修订记录/检验项目及标准/成品及包装运输);检验项目及标准页从标准库
  (测试项目汇总 26 类 48 子项)勾选组装;规格书种类属配置类数据,译名人工维护不机翻。
  **整份按真实 A4 纸呈现**:面板网格宽 = A4 宽(210mm @96dpi = 794px),封面/各页均按
  k=网格宽÷各自设计画布宽 等比缩放,字号不出框。
  **修订记录页按设计图像素级还原**:dt.design 设计像素规范(标题字号/上下间距/表头行高/数据行高/字号,
  k=网格宽÷表格设计宽 等比缩放),表头白底正常字重,单元格居中,行高固定;
  **检验项目及标准页 = 标准库勾选驱动**:内容不固定,全部由分组标准库(yj_std_lib `spec.test`,
  原内置 26 组/48 子项已种子入库,源《测试项目汇总.xlsx》→ tools/gen-spec-testlib.cjs)勾选填入;
  表格为分组式(序号按组编号、检验项目组跨行 + 子项目列、要求/方法/依据左对齐可编辑),存储以「组·子项」
  写入检验项目字段,渲染按前缀分组;
  **标准库(yj_std_lib,可增/可编辑/可停用/可恢复)**:`yj_std_lib(lib_code, item_code, content)` 存标准库条目,
  StdLibController 提供列表/新增/编辑/停用/恢复启用——库不写死,用户勾选使用、可存新条目长期复用。
  维护界面统一为「**勾选一行 → 点『编辑』行内改 / 『停用』/『恢复启用』**」(共用组件 `StdLibManager.vue`),
  停用是软删(enabled=0),维护列表里灰显可见、可救回;业务下拉只出启用条目。
  **改库条目不污染已录入数据**:面板勾选录入时,单据列存的是**内容文本**(不存条目 id/编码),所以编辑或
  停用库条目只影响以后的勾选候选,历史单据原样不动(验收探针 `tools/_probe-stdlib-edit.cjs` 钉这条)。
  规格书章节标准库(spec.section):1.适用范围/2.整体规格参数/3.产品主要性能/6.包装方式/7.运输要求/
  8.存储环境 各章节的示例条目(《规格书示例》10 份提取 24 条种子);章节行「标准库」按钮勾选填入;
  7./8. 另有通用文案默认预填(sectionDefaults)。
  **检验项目标准库(2026-09-11 反转口径)**:规格书用 `spec.test`、出货检验计划表用 `insp.plan`,
  **两库各自维护、不共用条目**——一边新增的条目不会出现在另一边的弹窗里。两库正文统一为**规范结构 v2**
  (真源 `core/panel/testItemLib.js`,读取兼容旧格式、编辑保存时升级)。**2026-09-11 晚重构:
  两库全部条目入库(yj_std_lib 唯一真源),内置常量降级为种子数据源+未迁移环境兜底**——
  原内置条目(规格书 26 组/48 子项、必测项 7 + 型式检验 6)经 `tools/gen-testlib-seed.cjs`
  生成的幂等种子 SQL(`migrate-testlib-seed.sql`)入库,库条目(含种子迁入的原内置项)一律
  「勾选 → 编辑」可改、✕ 停用、↩ 恢复启用,与 StdLibManager 同款维护能力。
  不共库的来历与理由见「已敲定决策」2026-09-11 两条。
- **出货检验计划表**(RD_INSP_PLAN):信息块仅版本号/密级;产品编号/客户名/管理人 + 主要性能/
  滤芯尺寸/授权使用人 两行信息;必测项(7 项)与型式检验(6 项)两套标准库(yj_std_lib `insp.plan`,
  item_code=表区名,原内置种子已入库)勾选组装;
  全页共用 10 列网格(产品编号↔控制项目、客户名↔控制标准及要求 分界线对齐)。
- **检验项目标准库**(Inspection Item Standard Library):面板里「勾选检验项目」用的候选条目库,
  **规格书(RD_SPEC_DOC)与出货检验计划表(RD_INSP_PLAN)各自维护自己的库,不共用**
  (2026-09-11 反转先前的共库决定):规格书「检验项目及标准」= `spec.test`(item_code=组名);
  出货检验计划表「必测项/型式检验」= `insp.plan`(item_code=表区名,按表区过滤)。
  一边新增的条目**不会**出现在另一边的弹窗里。
  条目正文统一用**规范结构** `v=2`(`core/panel/testItemLib.js`,唯一真源:读取 `toCanonical`、
  写回 `toContentJson`、投影 `toSpecSub`/`toInspRow`):读到历史旧格式(规格书 `{sub,req,method,basis}` /
  出货检验计划表 10 个中文键)按兼容规则解析,**用户「勾选 → 编辑 → 保存修改」时升级成 v2**——
  所以**旧格式条目同样可以编辑**,不存在"老数据改不动"一说。
  规范结构的字段是两面板字段的**并集**(规格书:组名/子项目/检验要求/检验方法/检验依据;
  出货检验计划表:控制项目/质量控制内容/检测仪器/控制标准及要求/检验/不合格应对措施/检测频率/
  取样方式/检验内容/控制方法),各自投影出自己的列,本面板没有的字段**留空不丢**
  (编辑保存时以条目的原规范结构打底,不拿空串覆盖)。组名是条目的归属(存 item_code),编辑不改组名;
  **两库条目全部来自 yj_std_lib**(2026-09-11 晚起,原内置常量已种子入库,见上方重构说明),
  有 dbId 即可编辑/停用/恢复;仅"未跑种子环境的内置常量兜底"行没有 dbId、不可维护。
  (重名提醒:这是**检验项目**标准库;实验室 4 库 lab.* 与规格书章节库 spec.section 不在此列。)

数据表列宽:数据表列带 `w` 时自持 colgroup(与面板网格解耦);缺省按面板网格跨度对齐。

### 附件字段(Attachment Field)
文书面板头字段的一种控件形态(recordSheetConfigs `type:'file'` → FileAttachCell):字段值不再是
手填文本,而是挂在单据上的附件集合。**锚点=面板编码+单据编号+字段键**(`yj_attachment` 表),
文件本体存后端磁盘(存储名=UUID+安全扩展,杜绝路径穿越),元数据入库并**保留原文件名**。
交互:多附件上传(不限类型、单文件≤20MB)、点文件名查看(图片/PDF/文本浏览器内联打开,
其余按原文件名下载)、可删除;**新建未保存的单据(无单据编号)不可上传,须先保存**。
头字段列由服务端在每次上传/删除后同步为**文件名列表(、分隔)**——列表、纸张打印、导出 PDF
(所见即所得)都只显示文件名,不出现文件本体;历史遗留的手填文本在无附件时原样只读展示。
首个使用方:产品信息表「客户图纸或规格书」(源 Excel 该格提示即"上传图片")。

### 项目阶段进度(Stage Progress)
项目实施计划(RD_PLAN)的"测试计划"用 10 个阶段框承载项目阶段,每个阶段 5 个字段:
`阶段N_计划内容 / 阶段N_计划开始 / 阶段N_计划完成 / 阶段N_实际完成 / 阶段N_责任人`
(`阶段N` 为旧版单字段标题,仅作计划内容为空时的兜底)。
**完成口径**:有效阶段 = 计划内容(或旧标题)非空,空白阶段框不计入;已完成 = `实际完成` 非空;
逾期 = 未完成且 `计划完成` 早于今天(当天不算逾期)。
**项目进度查询(RD_PROGRESS)的「状态」列是派生快照**(只读、不手填):按行上的项目名称关联
最新一张同名实施计划(同名多张取单据编号最新),算成 `未开始 0/5 · 进行中 2/5 · 进行中 2/5 · 逾期 1 ·
全部完成 5/5 · 无阶段计划 · 无实施计划`;载入时写回行模型,所以列表/导出/入库口径一致。
点状态标签弹出**只读**《项目阶段计划》窗口:逐阶段列计划内容/开始/完成/实际完成/责任人 + 已完成/
进行中/逾期徽标 + 汇总(已完成 n · 未完成 m · 逾期 k)与下一阶段;登记完成仍走实施计划纸面的「完成」按钮,
不在弹窗里写数据(避免两处都能改同一个值)。

### 生产过程(Production Process)
混料→成型→**切炭**→组装→**装箱**是**同一生产过程的五道工序**(一个整体的不同操作),不是独立模块:
共用一张工单(混料阶段可多次打印),按工序扫码领料、PDA/手机端扫码报工;
切炭无独立领料(直接耗用成型产出),报工双出口(部分合格品直销入成品仓,其余继续组装);
**装箱为末道工序,装箱后成品入库**(封箱前有数量确认、装箱前有黑水测试)。
流程图画"裁切"、真实单据与车间名为"切炭"(切炭车间),术语以切炭为准;
"混料"真实载体为自制物料车间(阻垢颗粒料/活性粉制备)。
排产公式:成型计划按工艺单"1切几"折算,切炭/组装/装箱计划=订单数;完成数=工单报工数求和。

### 红字冲回(Red-letter Reversal)
弃审切炭报工单时,系统自动生成一张**负数量**的成品入库单(红字单)并审核过账——台账 rkl/yl 对称扣减,
原入库单保留(审计线索),红字单独立存在且备注注明冲回来源;form_flow_link 释放后重审可再生成新入库单。
库中留痕 = 正/红字成对出现(v_lot_trace 可见两条事件),总账恒等。
守卫:红字冲回若使现存量为负(库存已被下游消耗)则拒绝弃审,需先冲销下游单据。

### 产品流动标识卡(Product Flow Card)
纸质随货卡片,产品二维码/条码体系的取代对象。字段:产品批次/产品编码/产品规格/货物形态/
材料/生产日期/数量/负责人 ×2 组(两次流转记录)+ 第__框/共计__框。
二维码化后的追溯数据模型以此为基础(物料编码+批号,批号=入库日期+3位流水号)。

### ERP 对接(金蝶云·星辰 / jdy-sync)
ERP 为**金蝶云·星辰**(开放平台 open.jdy.com / api.kingdee.com,沙箱域 tf.jdy.com)。
已验证的同步实现 = 独立 Node 工具 **jdy-sync**(计划任务每 5 分钟):
双 HMAC-SHA256 签名(clientId/Secret 应用级 + appKey/Secret 账套级换 app-token,缓存 23h)→
全量拉取列表+逐单详情(**时间窗过滤实测漏单,禁用**)→ 字段映射中文列 → **单事务直写**
bd/bl 表,幂等锚点=`外部数据ID` 唯一索引(行表按单号先删后插);审核状态镜像到
`yj_doc_status`(shr/shsj/stopped),星辰为唯一事实源(MES 手动审核 5 分钟内被覆盖)。
已落地:销售订单(/jdy/v2/scm/sal_order → bd_so_order/bl_so_order,2026-09-08 实跑验证)。
踩坑:账套限流 500 次/分钟;路径含括号会致计划任务静默失败(部署到 D:\jdy-sync);bat 须 CRLF。
**本机(PANDA/Win11 26340)部署**:计划任务 JdySalOrderSync 直接以 node.exe(C:\nvm4w\nodejs)为动作,
每 5 分钟——**该构建拒绝 wscript.exe 作为任务动作(0x800710E0),README 的 vbs 隐藏方案不可用**;
node 走 nvm4w,勿硬编码 Program Files 路径;任务设置须放开电池供电限制。
erp_imp_log/erp_imp_row 通道表定位调整为**审计层**(同步器写批次摘要),不作为写入路径。

### 库存台账记账(Stock Ledger Posting)
单据审核 → kucun(物料编码 wzdm+仓库编码 ckdm+批号 lot_no 粒度)入账:rkl 累计入库/yl 现存量;
弃审对称冲回,现存量为负(库存已被消耗)则拒绝弃审整笔回滚。仓库以名称入单,记账时经 bs_wh 解析编码。
一期挂采购入库单(PURCHASE_IN);出库/产成品入库随各自层接入 StockLedgerService 同一服务。
明细缺存货编码/仓库档案不存在 → 审核拒绝并提示(强制基础数据先行)。

### 归档后申请修改(Archived Modification Request)
**全部文书归档面板**(保存即归档一族,真源 `ButtonService.DOC_ARCHIVE_PANELS` 21 面:立项申请/实施计划/
数据记录表 8/实验室使用记录表 4/产品文件 7,前端经面板配置 `metadata.docArchive` 驱动按钮显隐)的
单据修改闭环:已归档 ─任何人「申请修改」→ 修改申请中 ─管理员通过→ 修改中(可编辑,保存不自动归档)
─提交审批→ 审批中 ─通过→ 已归档;驳回任一步退回。2026-09-11 前仅产品文件 7 面板有此闭环,
当日放开到全部(实验室记录表行字段归档后可改即由此而来);后端动作本就面板无关,放开只动了前端开关。
修改记录(按钮)按"滚动3条"保留:第4次起覆盖最早;每条含 申请/修改审批/再归档 三段留痕、
头字段 diff(变化/补充/清空)与明细行变化摘要(新增/删除/修改行数,不展开字段)。
状态存储 `yj_doc_status.modify_state`('R'申请中/'Y'修改中),记录表 `yj_doc_modify_log`;
角色授权动作集(FILE_PANEL_ACTIONS 含 申请修改/修改记录/删除申请)同按 DOC_ARCHIVE_PANELS 下发。

### 文书归档面板(Archived Doc Panels)
**保存即归档的一族文书面板**,真源 `ButtonService.DOC_ARCHIVE_PANELS`,**共 21 面**:立项申请 1 +
项目实施计划 1 + 数据记录表 8 + 实验室使用记录表 4 + 产品文件 7(前端不维护清单,
经面板配置 `metadata.docArchive` 下发,新面在 `DOC_ARCHIVE_PANELS` 登记即自动获得归档与修改闭环)。
**研发管理 22 个面板里 `RD_PROGRESS`(项目进度查询)不在其中**:它是全部实施计划实时汇总进来的
唯一一张单据,**无归档、无修改闭环、永远草稿**——不是漏登记,是刻意排除(它的数据由
`syncAllPlansToProgress` 维护,归档语义无意义)。**本项目采用此含义。**

归档触发与审核人(既定行为,2026-09-11 确认,不是 bug):

- **管理员在草稿态点「保存」= 直接归档**(`markArchived`),不经审批;
  **普通用户点「保存」= 自动进审批中**(`pending='Y'` + 留痕 SUBMIT),管理员审批通过后归档。
- **归档时不写审核人 `shr`**——`markArchived` 只置 `archived='Y'`,所以归档单的纸面「审核人」栏
  **本来就是空的**(只有走审核/审批通过路径才写 `shr`);因此归档单在列表里**没有「已审批」角标**
  (角标判据看 `审批状态`,见「已审批判据」)。

**撤回申请(卡死单据出口,2026-09-11)**:删除申请(`deleting='Y'` → 删除申请中)与修改申请
(`modify_state='R'` → 修改申请中)提交后若不审批,单据既不可编辑也没有审批入口 = **卡死**。
两个撤回动作给出口:**「撤回删除申请」**(→ `deleting='N'`)、**「撤回修改申请」**(→ `modify_state=NULL`),
两者都回查真实状态(回到已归档),**允许申请发起人本人(`delete_req_by`/`modify_req_by`)或该面板
审批人**(管理员 ∪ `can_approve='Y'`),并各写一条 `yj_form_approval` 留痕
(action=`DELETE_WITHDRAW`/`MODIFY_WITHDRAW`,result=`WITHDRAWN`)。

### 标准库(Standard Library)

`yj_std_lib(lib_code, item_code, content)` 承载的可维护候选条目库。**本项目采用此含义。**
`lib_code` 区分用途:`lab.*` 实验室 4 库 / `spec.section` 规格书章节 / `spec.test` 规格书检验项目 /
`insp.plan` 出货检验计划表检验项目。条目可增、可编辑、可停用(软删 `enabled=0`)、可恢复;
**单据列存内容文本、不存条目 id**,故改库条目不污染已录入数据。

### 检验项目标准库(Inspection Item Standard Library)

规格书与出货检验计划表**各自维护自己的库、不共用**(`spec.test` 的 `item_code`=组名,
`insp.plan` 的 `item_code`=表区名 必测项/型式检验);正文统一规范结构 v2,旧格式读取兼容、编辑保存时升级。
**本项目采用此含义。**(2026-09-11 由"两面板共库"反转为"各自维护",早于该日的共库表述均为历史)

## 已敲定决策

| 日期 | 决策 | 来源 |
|---|---|---|
| 2026-08-30 | 语言范围:简体中文 + 英语(架构可扩展,后续加语言只加语言包) | grill 会话第 1 问 |
| 2026-08-30 | 数据翻译边界:字典翻、事实不翻(不接机器翻译) | grill 会话第 2 问 |
| 2026-08-30 | 翻译存储:前端语言包(UI)+ 数据库译名列(面板/字典)分层 | grill 会话第 3 问 |
| 2026-08-30 | 切换交互:顶栏下拉 + Alt+L 快捷键,按用户记忆(yj_user.locale),热切换不刷新 | grill 会话第 4 问 |
| 2026-08-30 | 语言扩展路线:**通用翻译表(任意语言=加行不加列)+ 动态语言列表 + 常用语言包人工维护 + 未覆盖词条阿里云翻译 API 实时兜底(结果缓存回翻译表)+ 管理员可校对**。UI 文案机翻可接受(ADR-0001 的"事实不翻"仅约束业务数据) | grill 会话 2026-08-30 晚 |
| 2026-09-02 | 使用记录存储:新建 `yj_usage_log`(登录+操作统一一张表),不复用旧系统 `s_log`、不展示其历史 | grill 会话「使用权限查看」第 1 问 |
| 2026-09-02 | 使用权限查看入口仅管理员可见(登录用户不可见);记录范围为业务动作+登录成功,不含查询/刷新类浏览动作 | grill 会话「使用权限查看」第 2、3 问 |
| 2026-09-05 | 产品文件 7 面板对照《2.产品文件》源文档(OneDrive Desktop 路径)全量走查:修正成型工艺清单检验要求键值对块错位、成型配方网格(标题7列+信息块2+3/配方表13格)、组装BOM(去默认信息块+修订记录补备注列+数据表自持列宽)、组装工艺补第21道工序并预填全文、出货检验计划对齐碱性版源(去编写人/审核人行);规格书版面与设计图已一致,仅补静态 en 词条 | 走查会话 2026-09-05 |
| 2026-09-09 | 总流程图 V1.2 整理:裁切报工双出口(合格品一部分自动入成品仓直销、其余继续组装);工单由 ERP 单据审核后自动生成导入 MES;待开发泳道与建议开发顺序见 `tools/_flow-v12/银嘉MES总流程整理-V1.2.md` | 流程整理会话 2026-09-09 |
| 2026-09-09 | 混料/成型/切炭/组装/装箱定位为**同一生产过程的五道工序**(整体,共用工单),不拆为独立模块;装箱为末道工序,装箱后成品入库 | 流程整理会话补充 |
| 2026-09-10 | 基础数据层四决策:仓库档案 bs_wh 唯一真源(CKDA 改挂,dm_ck 一次性迁移);WLBOM 改挂 bs_bom;物料检验标志=是否检验(bit)+检验方式(全检/抽检)两列;ERP 对接走 API 自动同步(通道表 erp_imp_log/erp_imp_row 骨架不变);批号=入库日期+3位流水,二维码=物料编码+批号 | 草案评审会话(tools/_flow-v12/基础数据层表结构草案-v0.1.md) |
| 2026-09-10 | ERP 确认为**金蝶云·星辰**,销售订单同步已由独立工具 jdy-sync 实现并实跑验证;后续 物料/BOM/采购单 同步沿用其直写+外部数据ID幂等模式,erp 通道表降级为审计层 | jdy-sync 源码走查会话 |
| 2026-09-10 | **V2.0 补充设计完成**(`tools/_flow-v12/补充设计-V2.0.md`):七模块覆盖 V1.2 全部差距——二维码体系(批号注册表 qr_batch_registry)/品检自动分流(bs_inv.是否检验+检验方式)/五工序模型(wo_stage_report+wo_material_pick+wo_line_stock+切炭双出口)/29份单据(RecordSheetPanels配置驱动)/隔离仓(CK06/CK07)/ERP双向(jdy-sync扩展+审核回写)/成本核算(cost_wo_summary)。四阶段路线图,Phase 1+2可并行 | V1.2 差距分析+补充设计会话 |
| 2026-09-10 | 品检分流三单落地(QC_RECV/QC_INSP/QC_RETURN,doc 面板+参照链);**推式生单扩展多来源+行过滤**(PUSH_DETAIL_FILTERS 按处置方式拆行:非退货→采购入库单,退货→暂收退回单);E2E 验证实收=合格数量/退货=不合格数量/批号贯通;表单 API 明细真实路径=data.detailData.items | 品检分流实施会话(tools/_e2e-qc4.py) |
| 2026-09-09 | 数据记录表「文档编号」改为**参照立项申请纸张右上角编号**(RD_APPROVAL.文档编号,8 面板统一,字段取消隐藏使其进入表头字段表):两张纸右上角编号一致;一张记录表对应一个已归档立项项目,同面板内唯一、跨面板可复用;立项申请右上角编号须各自唯一(模板号 YJ-XS002 需按项目改号);E2E 六项断言通过 | grill 会话 2026-09-09 |
| 2026-09-09 | **参照带回不覆盖本单标识字段**(refMap 排除分组列/单据号列/日期列/主键列):单据→单据参照撞同名标签会冲掉本单单据编号/日期并触发唯一性误判;业务字段(密级)仍自动带回 | grill 会话 2026-09-09 |
| 2026-09-09 | **右上角编号两侧必填**:立项申请与 8 张数据记录表的「文档编号」required=1,并删除列上的模板号 DEFAULT(YJ-XS002/YJ-PD-01)——默认值会让必填校验形同虚设且直接保存撞唯一性;立项申请表该字段同时取消隐藏(隐藏则校验跑不到);数据记录表参照弹窗只列**已归档**立项申请 | grill 会话 2026-09-09 |
| 2026-09-09 | **项目实施计划(RD_PLAN)右上角编号同样参照立项申请**:`RD_PLAN.文档编号` → 参照 `RD_APPROVAL.文档编号`(同口径:取消隐藏+必填+删模板号 DEFAULT);DocSheet 顶部条右上角支持参照控件;密级/文件管理人/文件使用范围随参照带回 | grill 会话 2026-09-09 |
| 2026-09-10 | **研发管理二级目录按研发流程整理为三组**:项目管理(立项申请/项目实施计划/项目进度查询)、测试记录(数据记录表 8 + 实验室使用记录表 4)、产品文件(7);二级目录不再「叶子项与分组混排」,面板/权限/中文数据键全不变(只改 `menus.js` 导航层级) | grill 会话 2026-09-10 |
| 2026-09-11 | **编号格提示词术语澄清**:立项申请(RD_APPROVAL)/项目实施计划(RD_PLAN)纸张右上角那一格的提示词与标签一律用「文档编号」(该格绑定的就是 `文档编号` 键),**不得写成「单据编号」**——同面板的「单据编号」是不可编辑的系统流水号(`s_allno` 生成),提示词指向它会把经办人引去填一个填不动的字段;11 个语言补齐「文档编号：」词条(zh-CN 为源语言不加),验收探针 5 项断言通过(含切英文重载验词典真命中,仅中文比对命中的是原串本身、证明不了词条生效) | grill 会话 2026-09-11 |
| 2026-09-11 | **文书面板「人」字段只读锁定**:立项申请新增时 `申请立项人`=当前登录用户姓名;项目实施计划新增时 `负责人`=当前登录用户姓名(**RD_PLAN 没有「申请立项人」字段,它的人是「负责人」**)。两字段 `yj_field.editable=0` → `buildMeta` 下发 `readonly`,新增弹窗/单据签名格/列表内联编辑三处只读。默认值真源 `frontend/src/core/panel/docDefaults.js`(原来散在 `PanelxList` 的草稿 watch 里:只覆盖 RD_APPROVAL,且根本**没有** RD_PLAN 的负责人分支)。「本次新增」判据用 `isFreshAddedDoc()`(库端 `saved='N'`,跨刷新可靠),打开既有单据一律不碰该字段——否则弃审后再打开会把申请人改成操作人(冒名)。验收探针 `tools/_probe-applicant.cjs` 9 项断言通过 | grill 会话 2026-09-11 |
| 2026-09-11 | **标准库条目可编辑维护**:原状是"只能增、只能停用(且停用后不可见也不可恢复)";现统一为「**勾选一行 → 编辑(行内就地改) / 停用 / 恢复启用**」。后端 `StdLibController` 新增 `POST /update`(只改 content,记 asp_user2/asp_time2)、`POST /enable`, `GET /list` 加 `all=1` 连停用条目一起下发并带 `enabled`(业务下拉仍只出启用条目);前端抽共用组件 `StdLibManager.vue`(实验室 4 库 + 规格书章节库接入,替换各自散落的增删逻辑与重复样式)。**口径:改库条目不污染已录入数据**——面板勾选录入时单据列存的是内容文本(不存条目 id/编码),所以编辑/停用只影响以后的勾选候选,历史单据原样不动;验收探针 `tools/_probe-stdlib-edit.cjs` 14 项断言通过,并用 SQL 复核单据值与其修改时间均未变 | grill 会话 2026-09-11 |
| 2026-09-11 | **产品信息表「客户图纸或规格书」改为附件字段**:首个 `type:'file'` 控件(FileAttachCell + `yj_attachment` 表 + `/api/attachment/*`)。决策链:①多附件(图纸与规格书可分挂);②锚点=面板+单据编号+字段键,**新建未保存不可上传,须先保存**(免临时键再绑定的复杂度);③不限文件类型、单文件≤20MB(multipart 上限 10→20MB,OCR 图片仍 10MB 服务端校验);④文件存磁盘(存储名 UUID+安全扩展)+元数据入库,**原文件名保留**;⑤头字段列由服务端上传/删除后同步为文件名列表(、分隔,nvarchar(500) 截断兜底)——**打印/导出 PDF 所见即所得只显示文件名**;⑥历史手填文本在无附件时原样只读展示 | grill 会话 2026-09-11 |
| 2026-09-11 | **「归档后申请修改」闭环从产品文件 7 面板放开到全部文书归档面板**(实验室 4/数据记录表 8/立项申请/实施计划等 21 面):后端动作(申请修改/修改审批/修改记录/再归档)本就面板无关,唯前端把修改组按钮硬锁在 7 面板——改为面板配置 `metadata.docArchive` 下发(真源 `ButtonService.DOC_ARCHIVE_PANELS`,与权限动作集同一真源),前端不再维护清单,新文书面板登记即自动获得闭环;实验室记录表归档后行字段不可改的问题由此解决;普通单据(RKD 等)不受影响。验收探针 `tools/_probe-modify-loop.cjs` 14 项断言(完整闭环+数据记录表放开+产品文件回归+普通单据不误伤) | grill 会话 2026-09-11 |
| 2026-09-11 | **检验项目标准库重构:两库条目全部入库、全量可维护**(规格书 spec.test 48 条 + 出货计划 insp.plan 13 条,含原内置):①内置常量降级为种子数据源+未迁移环境兜底,`tools/gen-testlib-seed.cjs` 生成幂等种子 SQL(NOT EXISTS 按 lib+item+name+**quality** 去重——出货计划同组多条靠质量控制内容区分,缺 quality 会误杀;缺键必须落 N'' 不能变 N'undefined' 否则全量重插);②前端 openLib 改 DB 唯一真源(all=1 取含停用),停用条目灰显划线+「已停用」标注+checkbox 禁用+↩ 恢复启用,与 StdLibManager 同款维护能力;③编辑(勾选→编辑→保存修改)放开到全部库条目(种子迁入的原内置项同样可改);④分组组序沿用内置常量 Excel 业务序(库内新组缀后);⑤改库仍不污染已录入单据。验收探针 `tools/_probe-testlib-maintain.cjs` 14 项断言 | grill 会话 2026-09-11 |
| 2026-09-11 | **检验项目标准库维护补齐三件**:①编辑表单长文本字段(检验要求/方法/依据/控制标准及要求/检验内容/控制方法)改 textarea **autosize 随内容增高 + 加宽 200→460px**;②条目「**彻底删除**」(🗑 物理删,确认弹窗,`POST /stdlib/destroy`,与「停用」软删相对,已录入单据不受影响——用户自删重复条目的正式途径);③本地库核实:两库纯种子(48+13)无互通残留、实验停用行已恢复启用;生产去重脚本 `tools/migrate-testlib-dedup.sql`(库内完全重复保最小 id,幂等)。踩坑:RecordSheetPanels 漏 `import ElMessageBox` → 🗑 点击静默失败(async 内 ReferenceError 走 unhandled rejection,不进 console.error),CDP 逐步诊断才定位 | grill 会话 2026-09-11 |
| 2026-09-11 | **功能性滤效(RD_FILTER_EFF)动态样品列**:样品列 2~6 列可调(默认 2),**＋/－ 控制条在表格外**(1.基本信息 上缘右侧,打印隐藏);计数存头字段「样品数」,物理列预置到 样品6(头:样品信息/测试装置及编号;明细:压力/流速/出水/去除率,`tools/migrate-filter-eff-samples.sql` 幂等);联动范围=样品信息行 + 测试装置及编号行 + 数据表三个样品组(取样前压力/流速、出水含量、去除率,组 colspan=列数);**总表宽恒定**(组总宽÷列数,1180px 不变);**减列清空被减列数据**(用户口径,确认弹窗);上限 6(列宽下限保护)。验收探针 `tools/_probe-eff-samples.cjs` 19 项断言(含保存归档后 样品数/第3列值 持久、归档只读按 N 列渲染、控制条隐藏) | grill 会话 2026-09-11 |
| 2026-09-11 | **成型配方并入成型工艺清单、组装BOM表并入组装工艺清单(一张单两个页签)**(用户口径):合并主体是工艺清单(RD_MOLD_PROC / RD_ASM_PROC),配方/BOM 作为第 2 页签;被并入的 RD_MOLD_FORMULA / RD_ASM_BOM **从菜单下线但元数据与权限行保留**(便于回滚),两面板的已有单据备份后清理(备份表 `<表>_bak_20260911`);组装侧补 `RD_ASM_PROC.产品编号` 参照字段(与成型侧同口径,产品开发矩阵据此从 5 列收敛为 4 列:成型工艺清单/组装工艺清单/规格书/出货检验计划表);打印沿用规格书行为**一次只打当前页签**。**存储方案:页 2 内容全部落在主面板自己的头表/行表**——头字段复用主面板同名头列(同一张纸同一个头),配方表/BOM 两张逻辑表落主面板行表并用「表区」分块(filterKey 模式),只为确实缺的列补物理列(成型补 配料要求+行表配方列;组装补 产品种类/整体规格外径·长度/成品重量+行表 BOM 列),**不新建物理表、不改已有单据语义**。代价与限制见「产品文件文书面板」的共表口径与验收探针 `tools/_probe-merged-panels.cjs` | grill 会话 2026-09-11 |
| 2026-09-11 | **组装工艺清单两页签调换:BOM 在前(页1=组装BOM表,页2=组装工艺清单)+ BOM 页报告头对齐修复**:①页归属翻转用 map 就地改写 `page`(BOM 块→0/工序表→1),`RD_ASM_BOM` 原配置保持逐字不动(回滚参考);②修复 BOM 页大标题挤进第一列(130px)+右侧冒出默认信息块(密级/适用范围/测试负责人/报告编号)的错位——根因:合并面板未声明 `info`,渲染器回退 DEFAULT_INFO;修法:**`pages[i].info`/`pages[i].head` 支持按页声明**(页声明空 `info:[]` = 原版无信息块,大标题 colspan=nCols 跨全宽)。验收探针 `tools/_probe-asm-swap.cjs` 9 项断言(页签顺序/标题 1040px 跨全宽/无信息块/两页内容与边界) | grill 会话 2026-09-11 |
| 2026-09-11 | **规格书检验项目变更 → 出货检验计划表核对提醒**:①**关联键=规格书编号=计划表产品编号**——注意:规格书的「编号」在行模型里就是单据编号(查询服务注入,物理头表无独立编号列),故关联直接以 specNo 匹配 `rd_insp_plan_head.产品编号`;②**仅变更提醒不比对**(两侧检验项名称体系不同,机械比对易误报,差异由经办人人工核对);③**触发时机=修改态保存且「检验项目及标准」页(表区=检验要求)发生变化**——保存前快照(检验项目◇要求◇方法◇依据 按 id)vs 落库后比对,增/删/改/调序任一变化触发,无关改动保存不重复打扰;④通知走 yj_message(消息码 SPEC_ITEMS_CHANGED,收件人=计划表经办人+管理员),消息中心渲染「规格书检验项目已更新…请核对本表的必测项/型式检验」,10 语言词条。验收探针 `tools/_probe-spec-notify.cjs` 11 项断言(含不匹配计划表不收、无变化保存不重发)。踩坑:探针漏 `/api` 前缀时 404 静默变空数组 | grill 会话 2026-09-11 |
| 2026-09-11 | **项目实施计划「申请终止(阶段处)」二级审批**:申请(阶段N,原因选填)→ **P1 待立项人审批**(立项人=本计划文档编号所引立项申请的「申请立项人」,按 yj_user.real_name 匹配登录账号,**严格口径:管理员不代审,无账号则挂起**,申请人可撤回)→ 同意后 **P2 待管理员审批** → 同意后 **T 已终止(落实:单据锁定——不可保存/阶段完成/再申请;阶段数据与留痕保留)**;任一级驳回或发起人/管理员撤回 → 删行回到原状态可重新申请。存储 `yj_plan_term`(一单一行,state P1/P2/T,`tools/migrate-plan-term.sql`);留痕 `yj_form_approval`(TERM_REQUEST/L1·L2_APPROVE·REJECT/WITHDRAW);状态推导**两处都要接**(ButtonService.docStatusOf 与 QueryService.loadStatus/docStatus,后者管列表行状态);UI:DocSheet 终止横幅(审批中蓝/已终止红,打印隐藏)+ 阶段行「申请终止」按钮 + `/px/planTerm` 查询;消息 TERM_REQUESTED/TO_ADMIN/APPROVED/REJECTED;46×10 语言词条。验收探针 `tools/_probe-plan-term.cjs` 24 项断言(admin 一级被拒/立项人二级被拒/锁定/消息/驳回/撤回)+ UI 冒烟。踩坑:①`char(2)` state 右补空格,比较一律 trim/RTRIM;②API 建计划只传头字段会落 directAdd 空草稿分支(不归档),须先建草稿再带编号二存;③RD_PLAN 同面板文档编号唯一,一立项申请只挂一张计划 | grill 会话 2026-09-11 |
| 2026-09-11 | **项目进度查询·项目编号链接 → 该项目数据记录表单据(弹窗就地只读渲染,不跳转;用户改口径,替代首版的跳转 focus 方案)**:进度明细「项目编号」列(落库键=说明=计划文档编号=立项申请文档编号)加 📄 链接(打印隐藏,编辑/只读态均可点)→ 弹窗列出该项目在**数据记录表 8 面板**的全部单据并**直接渲染单据内容**:多张时顶部胶囊切换条(面板名+单号),单张自动展示;功能性滤效用 `DataRecordSheet`、其余 7 张用 `RecordSheetPanels` 渲染(editable=false,字段元数据按目标面板 getPanelConfig 现取缓存)。数据拼装:**getFormDescriptor 响应是双层嵌套**——`body.data.data`=头字段、`body.data.detailData`=明细,行模型={...data, detail: detailData}。聚合端点 `GET /px/progress/dataSheets?code=<项目编号>`(ButtonService.progressDataSheets 循环 8 面板查同号文档编号,作废不计)。验收探针 `tools/_probe-progress-sheets.cjs` 13 项断言(造数→聚合→点链接→就地渲染两张不同面板纸张→胶囊切换内容变→不跳转→零错误)。注意:DataRecordSheet 只读态右上编号格显示**文档编号优先**(=项目编号) | grill 会话 2026-09-11 |
| 2026-09-11 | **【已反转,见下行】** **检验项目标准库两面板互通**:规格书与出货检验计划表**共用同一批条目**(统一挂 `spec.test`,旧 `insp.plan` 库由 `tools/migrate-testlib-shared.sql` 并入),字段不同之处**做一层映射合并成一套共用条目**(纯模块 `core/panel/testItemLib.js` 的规范结构 v2:`toCanonical/toSpecSub/toInspRow/toContentJson`),**空字段留空、不丢数据**;条目正文兼容旧 JSON 形状(只在用户「编辑→保存修改」时升级为 v2);两面板均支持「勾选一条自定义条目 → 编辑 → 表单带入 → 保存修改」(`POST /stdlib/update`),内置常量条目无 dbId 不可编辑;组名存 item_code,编辑不改组名 | grill 会话 2026-09-11 |
| 2026-09-11 | **反转上一条:取消两面板共库,改为各自维护自己的库**(用户改口径)。规格书= `spec.test`、出货检验计划表= `insp.plan`(读取按表区 `item_code` 过滤,必测项/型式检验各看各的),一边新增的条目不会出现在另一边的弹窗里;写入路径 `addCustomTestLib`→`spec.test`、`addCustomFlatLib`→`insp.plan`(`POST /stdlib/update` 只认 id,与库无关,不变)。**编辑能力原样保留**:两弹窗照旧「勾选一条自定义条目 → 编辑按钮 → 表单带入 → 保存修改」,内容**继续走 `testItemLib.js` 规范结构 v2、不退回旧格式**;规范结构是**格式统一**而非数据共用,所以历史旧格式条目(规格书 `{sub,req,method,basis}` / 出货计划 10 中文键)读取兼容、编辑保存时升级为 v2 —— **旧数据不存在"不能编辑"一说**(这也是本轮反转的前提:编辑能力已经做出来了,再谈库分不分)。共库迁移 `tools/migrate-testlib-shared.sql` 从未在服务器执行过、本机执行搬运行数为 0,故**删除文件并从 `tools/db-migrations.txt` 撤登记,不写反向迁移**;SQL 复核 `yj_std_lib` 无因它错位的行(spec.test/insp.plan 现各 0 行)。验收探针 `tools/_probe-testlib-edit.cjs` 24 项断言通过(旧格式条目经界面编辑→保存后回读为 v2、表单上没有的 10 个字段一个不丢 + 两库互不相见 + 清理后行数复原),`npm test` 112 通过、`npm run build` 通过 | grill 会话 2026-09-11 |
| 2026-09-11 | **文书面板审批与归档口径确认 + 一轮实现/验证修复**(详见「文书归档面板(Archived Doc Panels)」):①**审核/弃审补审批权校验**——`audit()`/`unaudit()` 起手 `requireApprover`(管理员 ∪ `yj_role_panel.can_approve='Y'`),与审批通过/驳回同口径;②删除审批(`approveDelete`/`rejectDelete`)补 `yj_form_approval` 留痕(新增 action `DELETE_APPROVE`/`DELETE_REJECT`);③`rejectDelete`/`modifyReject` 不再硬编码返回"已归档",改回查真实状态;④**文档编号查重排除已作废单据**(业务表 `asp_cancel='Y'` + 状态表 `canceled='Y'` 两侧都排),作废掉再新建同号不再撞唯一性;⑤`markSaved` 死参数落实生效(保存/提交写 `saved='Y'`,新增/保存为草稿写 `'N'`);⑥两份设计文档里"单号释放后可重发"的注释纠正为"历史清理脚本物理删头行导致孤儿状态行";⑦**新增两个卡死单据出口**(`撤回删除申请`/`撤回修改申请`,发起人或审批人可撤),并用它把库里两张实测卡死的删除申请单(`MP-2026-09-0005`/`MP-2026-09-0007`)救回已归档(`CP382S` 的修改申请按用户口径未动,新增的出口已可供人工撤回);⑧列表页「已审批」判据统一为 `['已审批','已通过']`(原单值 `=== '已审批'` 永不命中)。**口径确认(既定行为,不是 bug)**:管理员草稿态点「保存」= 直接归档不走审批,普通用户点「保存」= 自动进审批中;**归档不写审核人 `shr`**,故归档单纸面审核人栏为空、列表无「已审批」角标;**`RD_PROGRESS`(项目进度查询)刻意不在 `DOC_ARCHIVE_PANELS`(21 面)内**,无归档/无修改闭环/永远草稿 | grill 会话 2026-09-11 |
| 2026-09-11 | **【反转上一条】两对面板恢复成「各自原版式 + 同一张单两个页签」**(用户反馈:「组装bom表和组装工艺清单看起来都结构都乱了,完全设计坏了,你就按一开始怎么样子设计的把两个面板用tap形式组合在一起就好了……这两个没有字段可以关联起来可以不强关联,但是在同张单据里面联系就行了」)。根因:首次合并把两页的 `cfg.grid` **统一成 11 列/1040** 并把页 2 区块跨列重排,而引擎 `effGrid` 原先只认 `cfg.grid`(区块无 `sec.grid` 回退)⇒ 两页原本不同的列数与列宽被改坏。修法:①引擎新增**每页版式** `pages[i].grid / headMode / head / showHead / staticTitle`(退化顺序 `pages[i].x → variants[].x → cfg.x`;未声明项与改造前逐字等价);②四个面板恢复成 e20bd9a~1 的原文——页 1 用工艺清单原 grid(成型 11 列 / 组装 plain 自持 130·320·430·120、表宽 1000),页 2 用被并入面板原 grid(成型配方 13 列 + 报告头跨度 7/2/4;组装BOM 4 列 130·390·130·390 + report 报告头),两页各显示自己的大标题;③被并入面板配置从记录表对象**提成模块级常量**(`RD_MOLD_FORMULA` / `RD_ASM_BOM`,对象字面量里同级属性名取不到),内容一字不动、只摘面板级 `headMode` 并补 `page:1`——同一份文本既是页 2 的渲染来源又是天然的回滚参考;④页 1 自身区块/数据表同样逐字保留(`RD_MOLD_PROC_SEC0` / `RD_ASM_PROC_DT0`)。**顺带修掉首次合并遗留的隐性数据缺陷**:配方表并入共用行表后必须挂 `表区=配方表` 分块键,否则新增配方行以 `表区=NULL` 存进去、下次打开按表区过滤一行都读不回来(探针实测到"行存在但看不见")——这是功能元数据、不是版式,故单独补上并单列断言。数据落点、菜单、`DevTaskService.DEV_PANELS`、`DOC_ARCHIVE_PANELS`、权限行一律未动;无需新迁移(页 2 所需物理列与 yj_field 在首次合并时已补齐)。配置层校验 46 项 + 端到端探针 `tools/_probe-merged-panels-layout.cjs` 51 项全绿(核心断言=两页实际渲染列宽数组逐项 ≡ 合并前原文 grid),`npm test` 112 通过、`npm run build` 通过。**旧探针 `tools/_probe-merged-panels.cjs` 随之失效**(它断言的正是被反转的 11 列/跨列重排口径) | grill 会话 2026-09-11 |

### 已审批判据(Approved Status Criteria)

单据"已审批"的统一判据(对应 light-mes `core/doc-status.js` 语义;YINJIA 由
`yj_doc_status` 状态推导实现同一判定):状态 ∈ {已审核, 生产中, 已完工, 已关闭}
或 审批状态 ∈ {已审批, 已通过}。列表/表单共用同一判据,禁止各处手写状态集合。

- 落地现状(2026-09-11 校正):列表页「已审批」角标与明细行浅绿底色**只按 `审批状态` ∈ {已审批, 已通过}**
  判定(`PanelxList.vue` 的 `APPROVED_STATUS_VALUES`)。后端 `QueryService` 只产 `'已通过'`(shr 非空)
  与 `'审批中'` 两值——**单值比较 `=== '已审批'` 是错的**(永不命中,角标曾经完全不显示)。
  保存即归档的单据(无 `shr`)**按设计不带角标**,故**不**把"状态 ∈ {已审核, …}"那半句并进列表页判据;
  需要"这张纸归档了没有"请看 `单据状态` ∈ {已归档, 已审核},不要用角标代替。

### 翻译表(Translation Table)

`yj_translation(scope, ref_key, locale, text)`:任意对象(面板/字段/前端词条)的任意语言译名存行,
替代按语言加列(label_en/label_ja...)的不可扩展模式。`yj_locale` 为语言注册表(可启用语言列表)。
机翻兜底:翻译表 miss 时调阿里云翻译(与 OCR 同厂同凭证),结果写回翻译表成为缓存。

## 遗留问题(记录在案,未决)

- Element Plus 组件内置文案(日期选择器、分页等)需配 `zhCn`/`en` locale 同步切换
- 后端返回的消息(错误/成功提示)采用"消息码+参数"模式由前端翻译;存量硬编码中文消息需逐步迁移
- 日期/数字格式(本地化)随 locale 切换的粒度待实施时定
