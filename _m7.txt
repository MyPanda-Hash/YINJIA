fix: 库存报表漏单——MES 界面审核的出入库单从未进报表(视图口径以状态机为准,两账套)

用户报:找不到 PI-2026-09-0143 的入库数据。查证:
- 该单 yj_doc_status.shr=admin(状态机已审核)但头表物理列 单据状态=草稿——
  v_stock_movement 八段 WHERE 只认头表列(已审核/单据状态2=C),而那两值只有金蝶同步会写;
  MES 界面审核只写状态机表 → 本地审核的出入库单全部进不了三张库存报表。
- 受影响 6 张:PI-2026-09-0006/0088/0089/0115/0128/0143(全部 admin 审核)。
修复:migrate-inv-report-fields 八段 WHERE 各加
  OR EXISTS(yj_doc_status shr IS NOT NULL)(panel_code 分段对应),以状态机为准;
  两账套视图重建 + DbSync 登记(失败 0)+「重算成本」全量 242 行(新纳入 7 行流水获得成本)。
验证:PI-0128 两行/PI-0143(华北工控仓×金橙客供盖 100×5.5)均入流水与台账;
  流水 235→242、采购入库 112→119、无成本流水 0、勾稽自检通过。